import { useState, useEffect } from 'react';
import { toast } from '../utils/toast';
import { api } from '../api';
import { HiArrowRight, HiCheck, HiStar, HiLightningBolt, HiMenuAlt3, HiX, HiTrendingUp, HiSparkles, HiLockClosed, HiClock, HiPlay, HiShoppingCart, HiBookOpen, HiShare, HiUser, HiMail, HiPhone, HiAcademicCap, HiCode, HiUserGroup, HiBell } from 'react-icons/hi';
import FooterSunMascot from './FooterSunMascot';
import Forum from './Forum';
import SubjectsPage from './SubjectsPage';
import CoursesPage from '../pages/CoursesPage';
import CourseDetailPage from '../pages/CourseDetailPage';
import MockExamsPage from '../pages/MockExamsPage';
import MockExamDetailPage from '../pages/MockExamDetailPage';
import MockExamTakingPage from '../pages/MockExamTakingPage';
import MockExamResultPage from '../pages/MockExamResultPage';
import LearningPath from './LearningPath';
import AITutorPage from '../pages/AITutorPage';
import FlashcardPage from '../pages/FlashcardPage';
import ExamBankPage from '../pages/ExamBankPage';
import LeaderboardTab from './LeaderboardTab';
import Header from './Header';

// Import photorealistic human images generated by AI
import teacherMathImg from '../assets/teacher_math.png';
import studentLearningImg from '../assets/student_learning.png';
import educatorsTeamImg from '../assets/educators_team.png';
import studentSuccessImg from '../assets/student_success.png';
import sunMascotImg from '../assets/sun_mascot.png';
import sunLogoImg from '../assets/sun_logo.png';

const SUBJECTS = [
  {
    id: 'toan',
    label: 'Toán Học',
    desc: 'Đại số, Hình học, Giải tích và các dạng bài thi THPTQG nâng cao.',
    count: '120+ đề',
    bg: '#5b75f3',
    emoji: '🦉',
    combos: ['A01', 'B00', 'D01'],
  },
  {
    id: 'ly',
    label: 'Vật Lý',
    desc: 'Dao động, Sóng, Điện từ — bám sát cấu trúc đề thi mới nhất.',
    count: '100+ đề',
    bg: '#52ad58',
    emoji: '🦊',
    combos: ['A01'],
  },
  {
    id: 'anh',
    label: 'Tiếng Anh',
    desc: 'Ngữ pháp, Đọc hiểu, Từ vựng trọng tâm thi THPTQG 2026.',
    count: '100+ đề',
    bg: '#db8142',
    emoji: '🐸',
    combos: ['A01', 'D01'],
  },
  {
    id: 'hoa',
    label: 'Hóa Học',
    desc: 'Hữu cơ, Vô cơ, Bài toán tính toán hóa học nâng cao.',
    count: '90+ đề',
    bg: '#cf6674',
    emoji: '🐙',
    combos: ['B00'],
  },
  {
    id: 'sinh',
    label: 'Sinh Học',
    desc: 'Di truyền, Tiến hóa, Sinh thái — ôn thi khối B chuyên sâu.',
    count: '80+ đề',
    bg: '#6f4ab3',
    emoji: '🐢',
    combos: ['B00'],
    isFree: true,
  },
  {
    id: 'van',
    label: 'Ngữ Văn',
    desc: 'Nghị luận xã hội, Nghị luận văn học, Phân tích tác phẩm.',
    count: '70+ đề',
    bg: '#4598a7',
    emoji: '🦋',
    combos: ['D01'],
  },
];



const FEATURES = [
  { icon: '🔍', title: 'Chẩn đoán lỗ hổng tức thì', desc: 'AI phân tích kết quả và chỉ ra đúng kiến thức bạn đang yếu sau mỗi bài làm.' },
  { icon: '🗺️', title: 'Lộ trình học cá nhân hóa', desc: 'Không học dàn trải. Bản đồ học tập tự cập nhật theo điểm yếu của bạn mỗi ngày.' },
  { icon: '📝', title: 'Sinh đề sửa sai chuyên biệt', desc: 'AI tạo bộ câu hỏi tập trung đúng dạng bài bạn sai để luyện tập khắc phục ngay.' },
  { icon: '📊', title: 'Theo dõi tiến độ trực quan', desc: 'Biểu đồ điểm số theo tuần, streak học tập, dự đoán điểm THPTQG theo phong độ thực tế.' },
];

const getSpikePath = () => {
  const points = [];
  const numSpikes = 36;
  const cx = 300;
  const cy = 300;
  const rMin = 210;
  const rMax = 238;
  for (let i = 0; i < numSpikes; i++) {
    const angle1 = (i * 2 * Math.PI) / numSpikes;
    const angle2 = ((i + 0.5) * 2 * Math.PI) / numSpikes;
    points.push(`${(cx + rMax * Math.cos(angle1)).toFixed(1)},${(cy + rMax * Math.sin(angle1)).toFixed(1)}`);
    points.push(`${(cx + rMin * Math.cos(angle2)).toFixed(1)},${(cy + rMin * Math.sin(angle2)).toFixed(1)}`);
  }
  return `M ${points.join(' L ')} Z`;
};
const spikePath = getSpikePath();

const MOCK_LEADERBOARD = [
  { rank: 1, name: "Nguyễn Lâm Vy", combo: "A01 (Toán - Lý - Anh)", target: "28.5", actual: "28.2", streak: 42, badge: "Thủ khoa Lý 12", avatar: "LV" },
  { rank: 2, name: "Trần Minh Đức", combo: "B00 (Toán - Hóa - Sinh)", target: "29.0", actual: "28.4", streak: 35, badge: "Thần tốc Hóa Học", avatar: "MĐ" },
  { rank: 3, name: "Lê Quỳnh Chi", combo: "D01 (Toán - Văn - Anh)", target: "27.5", actual: "27.1", streak: 28, badge: "Đệ nhất Anh ngữ", avatar: "QC" },
  { rank: 4, name: "Phạm Minh Hoàng", combo: "A01 (Toán - Lý - Anh)", target: "28.0", actual: "26.9", streak: 21, badge: "Chiến thần Toán Học", avatar: "MH" },
  { rank: 5, name: "Đỗ Gia Bảo", combo: "B00 (Toán - Hóa - Sinh)", target: "27.8", actual: "26.5", streak: 17, badge: "Chuyên gia Sinh học", avatar: "GB" }
];

const FEATURED_COURSES = [
  {
    id: 1,
    title: "Ứng dụng đạo hàm khảo sát đồ thị hàm số",
    subject: "Toán học",
    price: "499.000",
    teacherName: "Thầy Thế Anh",
    duration: "32 giờ học • 45 bài giảng",
    rating: "4.8",
    students: "2,150",
    imageBg: "linear-gradient(135deg, #0984E3, #74b9ff)",
    lessons: [
      { name: "Bài 1: Sự đồng biến, nghịch biến của hàm số", duration: "18:45", isFree: true },
      { name: "Bài 2: Cực trị của hàm số và kỹ thuật bấm máy Casio", duration: "25:30", isFree: true },
      { name: "Bài 3: Giá trị lớn nhất, nhỏ nhất trên đoạn", duration: "22:15", isFree: false },
      { name: "Bài 4: Khảo sát sự biến thiên và vẽ đồ thị nâng cao", duration: "32:10", isFree: false }
    ]
  },
  {
    id: 2,
    title: "Chinh phục toàn diện Dao động cơ & Sóng cơ học",
    subject: "Vật lý",
    price: "599.000",
    teacherName: "Cô Thu Hương",
    duration: "40 giờ học • 55 bài giảng",
    rating: "4.9",
    students: "1,890",
    imageBg: "linear-gradient(135deg, #e17055, #fab1a0)",
    lessons: [
      { name: "Bài 1: Khái niệm Dao động điều hòa và các phương trình cốt lõi", duration: "20:15", isFree: true },
      { name: "Bài 2: Con lắc lò xo và bài toán năng lượng", duration: "26:40", isFree: false },
      { name: "Bài 3: Con lắc đơn và sự biến thiên chu kỳ", duration: "18:30", isFree: false }
    ]
  },
  {
    id: 3,
    title: "Ngữ pháp Tiếng Anh trọng tâm thi THPTQG 2026",
    subject: "Tiếng Anh",
    price: "399.000",
    teacherName: "Cô Quỳnh Chi",
    duration: "28 giờ học • 38 bài giảng",
    rating: "4.7",
    students: "950",
    imageBg: "linear-gradient(135deg, #00b894, #55efc4)",
    lessons: [
      { name: "Bài 1: Trọn bộ 12 thì trong Tiếng Anh và dấu hiệu nhận biết", duration: "15:45", isFree: true },
      { name: "Bài 2: Câu bị động và các dạng đặc biệt thường gặp", duration: "22:20", isFree: false },
      { name: "Bài 3: Câu điều kiện và Mệnh đề giả định", duration: "20:10", isFree: false }
    ]
  }
];

const autoWrapMath = (text) => {
  if (!text) return '';
  if (text.includes('$') || text.includes('\\(') || text.includes('\\[') || text.includes('$$')) {
    return text;
  }
  const latexPattern = /\\(frac|sqrt|vec|alpha|beta|gamma|delta|pi|theta|phi|omega|sigma|int|log|cos|sin|tan|lim|sum|prod|cup|cap|subset|subseteq|in|notin|neq|ge|le|approx|equiv|partial|nabla|to|rightarrow|left|right|cdot|text|mu|omega)/i;
  const mathCharsPattern = /[\^\\_]/;
  const isSimpleOptionFormula = text.length < 80 && (
    latexPattern.test(text) ||
    mathCharsPattern.test(text) ||
    /^[a-zA-Z\s]*[=><\-\+]/i.test(text) && (text.includes('^') || text.includes('/') || text.includes('*'))
  );
  if (isSimpleOptionFormula) {
    return `$${text.trim()}$`;
  }
  let processed = text;
  processed = processed.replace(/(?:\b|\s)(\([-+\infty\w\s]+;[-+\infty\w\s]+(?:\s*;\s*[-+\infty\w\s]+)?\))(?:\b|\s)/g, (match, p1) => {
    return match.replace(p1, `$${p1.trim()}$`);
  });
  processed = processed.replace(/([a-zA-Z_0-9']+(?:_[a-zA-Z0-9]+)?\s*(?:[=><]+|\\approx)\s*(?:[a-zA-Z0-9_+\-*\/\\^{}()[\]\s\.\u03c0\u221e]+))/g, (match, p1) => {
    if (/[\^\\_/\*\u03c0\u221e\(\)]|[a-zA-Z]{2,}\s+[a-zA-Z]/.test(p1) || p1.includes('\\') || p1.includes('^')) {
      let formula = p1.trim();
      const trail = formula.match(/[\.,;:\?]+$/);
      let suffix = '';
      if (trail) {
        formula = formula.slice(0, -trail[0].length);
        suffix = trail[0];
      }
      return ` $${formula}$${suffix} `;
    }
    return match;
  });
  processed = processed.replace(/(?:\s|^)([a-zA-Z0-9_+\-*\/\\^{}()[\]\.]*[\^\\_][a-zA-Z0-9_+\-*\/\\^{}()[\]\.]*)(?:\s|$)/g, (match, p1) => {
    if (!p1.startsWith('$') && !p1.endsWith('$')) {
      let formula = p1.trim();
      const trail = formula.match(/[\.,;:\?]+$/);
      let suffix = '';
      if (trail) {
        formula = formula.slice(0, -trail[0].length);
        suffix = trail[0];
      }
      return ` $${formula}$${suffix} `;
    }
    return match;
  });
  return processed.replace(/\s+/g, ' ');
};

const GUEST_MOCK_EXAMS = [
  {
    id: 'toan-12',
    name: 'Đề Luyện Thi THPTQG 2026 - Môn Toán (Mẫu thử)',
    subject: 'Toán học',
    subjectId: 'toan',
    questionsCount: 5,
    duration: 10,
    difficulty: 'Trung bình',
    attempts: '14,250',
    questions: [
      {
        id: 1001,
        question: "Cho hàm số y = x^3 - 3x^2 + 2. Khẳng định nào sau đây về tính đơn điệu của hàm số là ĐÚNG?",
        options: [
          { key: 'A', value: "Hàm số đồng biến trên khoảng (0; 2)" },
          { key: 'B', value: "Hàm số nghịch biến trên khoảng (0; 2)" },
          { key: 'C', value: "Hàm số nghịch biến trên khoảng (-∞; 0)" },
          { key: 'D', value: "Hàm số đồng biến trên khoảng (2; +∞) và nghịch biến trên (-∞; 0)" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Đạo hàm: y' = 3x^2 - 6x = 3x(x - 2).\ny' = 0 <=> x = 0 hoặc x = 2.\nLập bảng xét dấu y': trong khoảng (0; 2), y' < 0 => Hàm số nghịch biến trên khoảng (0; 2).",
        topic: "Sự biến thiên của hàm số"
      },
      {
        id: 1002,
        question: "Tìm tiệm cận đứng của đồ thị hàm số y = (2x - 1)/(x + 1):",
        options: [
          { key: 'A', value: "x = 2" },
          { key: 'B', value: "x = -1" },
          { key: 'C', value: "y = 2" },
          { key: 'D', value: "y = -1" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Tiệm cận đứng của đồ thị hàm số phân thức bậc nhất y = (ax+b)/(cx+d) là nghiệm của mẫu số: x + 1 = 0 <=> x = -1.",
        topic: "Đường tiệm cận của đồ thị hàm số"
      },
      {
        id: 1003,
        question: "Cho khối chóp có diện tích đáy B = 6a^2 và chiều cao h = 3a. Thể tích của khối chóp đã cho bằng:",
        options: [
          { key: 'A', value: "18 a^3" },
          { key: 'B', value: "6 a^3" },
          { key: 'C', value: "9 a^3" },
          { key: 'D', value: "2 a^3" }
        ],
        correctAnswer: 'B',
        difficulty: 'Trung bình',
        explanation: "Công thức thể tích khối chóp: V = 1/3 * B * h = 1/3 * 6a^2 * 3a = 6a^3.",
        topic: "Thể tích khối đa diện"
      },
      {
        id: 1004,
        question: "Họ nguyên hàm của hàm số f(x) = e^x + 2x là:",
        options: [
          { key: 'A', value: "e^x + x^2 + C" },
          { key: 'B', value: "e^x + 2 + C" },
          { key: 'C', value: "e^x + 2x^2 + C" },
          { key: 'D', value: "e^{-x} + x^2 + C" }
        ],
        correctAnswer: 'A',
        difficulty: 'Dễ',
        explanation: "∫(e^x + 2x)dx = e^x + x^2 + C.",
        topic: "Nguyên hàm - Tích phân"
      },
      {
        id: 1005,
        question: "Cho hàm số f(x) có điểm cực đại là x = 1 và giá trị cực đại y = 3. Điểm cực tiểu là x = 3 và giá trị cực tiểu y = -1. Khẳng định nào sau đây ĐÚNG?",
        options: [
          { key: 'A', value: "Hàm số đạt cực đại tại x = 3" },
          { key: 'B', value: "Giá trị cực tiểu của hàm số là -1" },
          { key: 'C', value: "Hàm số đạt cực tiểu tại x = 1" },
          { key: 'D', value: "Giá trị cực đại của hàm số là 1" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Hàm số đạt cực tiểu tại x = 3 với giá trị cực tiểu tương ứng là y_CT = -1.",
        topic: "Cực trị của hàm số"
      }
    ]
  },
  {
    id: 'ly-12',
    name: 'Đề Luyện Thi THPTQG 2026 - Môn Vật Lý (Mẫu thử)',
    subject: 'Vật lý',
    subjectId: 'ly',
    questionsCount: 5,
    duration: 10,
    difficulty: 'Trung bình',
    attempts: '11,400',
    questions: [
      {
        id: 2001,
        question: "Một vật dao động điều hòa theo phương trình x = A cos(ωt + φ). Đại lượng ω được gọi là:",
        options: [
          { key: 'A', value: "Tần số góc của dao động" },
          { key: 'B', value: "Pha ban đầu của dao động" },
          { key: 'C', value: "Chu kỳ của dao động" },
          { key: 'D', value: "Biên độ của dao động" }
        ],
        correctAnswer: 'A',
        difficulty: 'Dễ',
        explanation: "Trong phương trình dao động điều hòa x = A cos(ω t + φ), đại lượng ω là tần số góc của dao động.",
        topic: "Dao động điều hòa"
      },
      {
        id: 2002,
        question: "Một con lắc lò xo gồm vật nhỏ khối lượng m và lò xo nhẹ độ cứng k. Chu kỳ dao động riêng của con lắc bằng:",
        options: [
          { key: 'A', value: "T = 2π√(m/k)" },
          { key: 'B', value: "T = 2π√(k/m)" },
          { key: 'C', value: "T = (1/2π)√(m/k)" },
          { key: 'D', value: "T = (1/2π)√(k/m)" }
        ],
        correctAnswer: 'A',
        difficulty: 'Dễ',
        explanation: "Chu kỳ dao động của con lắc lò xo được tính bằng công thức: T = 2π√(m/k).",
        topic: "Con lắc lò xo"
      },
      {
        id: 2003,
        question: "Tại một nơi có gia tốc trọng trường g, một con lắc đơn chiều dài l dao động điều hòa. Tần số góc dao động riêng của con lắc bằng:",
        options: [
          { key: 'B', value: "ω = √(g/l)" },
          { key: 'A', value: "ω = √(l/g)" },
          { key: 'C', value: "ω = 2π√(g/l)" },
          { key: 'D', value: "ω = 2π√(l/g)" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Tần số góc dao động điều hòa của con lắc đơn: ω = √(g/l).",
        topic: "Con lắc đơn"
      },
      {
        id: 2004,
        question: "Sóng cơ truyền trên một sợi dây đàn hồi có bước sóng λ. Khoảng cách giữa hai bụng sóng liên tiếp là:",
        options: [
          { key: 'A', value: "λ" },
          { key: 'B', value: "λ / 2" },
          { key: 'C', value: "λ / 4" },
          { key: 'D', value: "2 λ" }
        ],
        correctAnswer: 'B',
        difficulty: 'Trung bình',
        explanation: "Khoảng cách giữa hai bụng sóng hoặc hai nút sóng liên tiếp bằng một nửa bước sóng: d = λ/2.",
        topic: "Sóng cơ và sự truyền sóng"
      },
      {
        id: 2005,
        question: "Một mạch dao động LC lí tưởng đang có dao động điện từ tự do. Điện tích cực đại của tụ điện là Q0, cường độ dòng điện cực đại trong mạch là I0. Tần số dao động của mạch bằng:",
        options: [
          { key: 'A', value: "f = I0 / (2π Q0)" },
          { key: 'B', value: "f = 2π I0 Q0" },
          { key: 'C', value: "f = Q0 / (2π I0)" },
          { key: 'D', value: "f = I0 Q0" }
        ],
        correctAnswer: 'A',
        difficulty: 'Khó',
        explanation: "Ta có mối liên hệ: I0 = ω * Q0 = 2πf * Q0 => f = I0 / (2π Q0).",
        topic: "Mạch dao động LC"
      }
    ]
  },
  {
    id: 'anh-12',
    name: 'Đề Luyện Thi THPTQG 2026 - Môn Tiếng Anh (Mẫu thử)',
    subject: 'Tiếng Anh',
    subjectId: 'anh',
    questionsCount: 5,
    duration: 10,
    difficulty: 'Dễ',
    attempts: '12,820',
    questions: [
      {
        id: 3001,
        question: "Identify the word whose underlined part differs from the other three in pronunciation:",
        options: [
          { key: 'A', value: "cooked" },
          { key: 'B', value: "checked" },
          { key: 'C', value: "planned" },
          { key: 'D', value: "laughed" }
        ],
        correctAnswer: 'C',
        difficulty: 'Dễ',
        explanation: "'planned' phát âm phần đuôi là /d/. Các từ còn lại phát âm là /t/ do kết thúc bằng âm vô thanh (k, ch, gh/f/).",
        topic: "Phát âm - Đuôi -ed"
      },
      {
        id: 3002,
        question: "He promised ______ home as soon as he finished his work.",
        options: [
          { key: 'A', value: "to come" },
          { key: 'B', value: "coming" },
          { key: 'C', value: "come" },
          { key: 'D', value: "came" }
        ],
        correctAnswer: 'A',
        difficulty: 'Dễ',
        explanation: "Cấu trúc động từ: promise + to-V (hứa hẹn làm điều gì đó).",
        topic: "Danh động từ & Động từ nguyên thể"
      },
      {
        id: 3003,
        question: "If I had known she was in hospital, I ______ her.",
        options: [
          { key: 'A', value: "would visit" },
          { key: 'B', value: "would have visited" },
          { key: 'C', value: "will visit" },
          { key: 'D', value: "visited" }
        ],
        correctAnswer: 'B',
        difficulty: 'Trung bình',
        explanation: "Câu điều kiện loại 3 (giả định trái ngược thực tế quá khứ): If + S + had + V3, S + would have + V3.",
        topic: "Câu điều kiện"
      },
      {
        id: 3004,
        question: "The weather is very hot, ______ we decided to go swimming.",
        options: [
          { key: 'A', value: "but" },
          { key: 'B', value: "so" },
          { key: 'C', value: "or" },
          { key: 'D', value: "because" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Liên từ 'so' đứng trước mệnh đề chỉ kết quả của hành động đứng trước.",
        topic: "Liên từ"
      },
      {
        id: 3005,
        question: "Identify the closest synonym of the underlined word:\nShe was 'elated' when she passed the high school national graduation exam.",
        options: [
          { key: 'A', value: "sad" },
          { key: 'B', value: "thrilled" },
          { key: 'C', value: "tired" },
          { key: 'D', value: "disappointed" }
        ],
        correctAnswer: 'B',
        difficulty: 'Trung bình',
        explanation: "elated (phấn chấn, vui mừng hân hoan) đồng nghĩa với thrilled (vui sướng, hồi hộp).",
        topic: "Tìm từ đồng nghĩa"
      }
    ]
  },
  {
    id: 'hoa-12',
    name: 'Đề Luyện Thi THPTQG 2026 - Môn Hóa Học (Mẫu thử)',
    subject: 'Hóa học',
    subjectId: 'hoa',
    questionsCount: 5,
    duration: 10,
    difficulty: 'Trung bình',
    attempts: '9,840',
    questions: [
      {
        id: 4001,
        question: "Chất nào sau đây thuộc loại este no, đơn chức, mạch hở?",
        options: [
          { key: 'A', value: "CH3COOH" },
          { key: 'B', value: "CH3COOCH3" },
          { key: 'C', value: "CH3CHO" },
          { key: 'D', value: "C2H5OH" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Este no, đơn chức, mạch hở có công thức chung CnH2nO2 (n>=2). CH3COOCH3 là metyl axetat là este điển hình.",
        topic: "Hóa học Este - Lipit"
      },
      {
        id: 4002,
        question: "Thủy phân hoàn toàn 1 mol chất béo (triaxilglixerol) trong dung dịch kiềm nóng thu được bao nhiêu mol glixerol?",
        options: [
          { key: 'A', value: "1 mol" },
          { key: 'B', value: "2 mol" },
          { key: 'C', value: "3 mol" },
          { key: 'D', value: "4 mol" }
        ],
        correctAnswer: 'A',
        difficulty: 'Dễ',
        explanation: "Thủy phân chất béo: (RCOO)3C3H5 + 3NaOH -> 3RCOONa + C3H5(OH)3 (glixerol). Do đó 1 mol chất béo sinh ra 1 mol glixerol.",
        topic: "Chất béo (Lipit)"
      },
      {
        id: 4003,
        question: "Kim loại nào sau đây có tính dẫn điện và dẫn nhiệt tốt nhất trong tất cả các kim loại?",
        options: [
          { key: 'A', value: "Nhôm (Al)" },
          { key: 'B', value: "Đồng (Cu)" },
          { key: 'C', value: "Bạc (Ag)" },
          { key: 'D', value: "Vàng (Au)" }
        ],
        correctAnswer: 'C',
        difficulty: 'Dễ',
        explanation: "Đạo dẫn điện giảm dần theo thứ tự: Ag > Cu > Au > Al > Fe.",
        topic: "Tính chất vật lý của kim loại"
      },
      {
        id: 4004,
        question: "Công thức hóa học của sắt (III) oxit là:",
        options: [
          { key: 'A', value: "FeO" },
          { key: 'B', value: "Fe2O3" },
          { key: 'C', value: "Fe3O4" },
          { key: 'D', value: "Fe(OH)3" }
        ],
        correctAnswer: 'B',
        difficulty: 'Dễ',
        explanation: "Sắt (III) oxit có công thức là Fe2O3. FeO là sắt (II) oxit, Fe3O4 là oxit sắt từ.",
        topic: "Hợp chất của Sắt"
      },
      {
        id: 4005,
        question: "Chất nào sau đây phân li hoàn toàn trong nước tạo ra ion (chất điện li mạnh)?",
        options: [
          { key: 'A', value: "NaCl" },
          { key: 'B', value: "CH3COOH" },
          { key: 'C', value: "H2O" },
          { key: 'D', value: "C2H5OH" }
        ],
        correctAnswer: 'A',
        difficulty: 'Trung bình',
        explanation: "NaCl là muối tan tốt, phân li hoàn toàn: NaCl -> Na+ + Cl-. CH3COOH là axit yếu, H2O điện li rất yếu, C2H5OH không điện li.",
        topic: "Sự điện li"
      }
    ]
  }
];

const FEEDBACK_LIST = [
  {
    name: 'Bạn Lý Gia Linh',
    school: 'Học sinh lớp 11A2 – THPT FPT Đà Nẵng',
    bg: '#FFF0F5',
    accent: '#EC4899',
    border: '#FBCFE8',
    text: 'Các học liệu tương tác về cơ bản em thấy đã sát với sách giáo khoa, nhưng được trực quan hóa một cách sinh động để em dễ hình dung và nhớ bài hơn.'
  },
  {
    name: 'Thầy Đào Anh Phúc – P20 Study',
    school: 'Tác giả SGK môn Sinh học lớp 12, người sáng lập P20 Study',
    bg: '#EBF3FF',
    accent: '#2563EB',
    border: '#BFDBFE',
    text: 'Phương pháp dạy học bằng mô hình 3D như EduPath AI có thể là một bước chuyển mình tích cực cho giáo viên trong bối cảnh siết chặt dạy thêm – học thêm hiện nay. Khi không còn nhiều thời gian bổ trợ ngoài lớp, việc tạo ra bài giảng sinh động, trực quan, dễ hiểu ngay tại lớp sẽ giúp học sinh tiếp thu tốt hơn, giáo viên cũng yên tâm hơn.'
  },
  {
    name: 'Ths Nguyễn Thành Công',
    school: 'Giáo viên môn Sinh học tại Trường THPT Chuyên Sư Phạm, ĐH Sư Phạm Hà Nội',
    bg: '#FFF8EB',
    accent: '#D97706',
    border: '#FEF3C7',
    text: 'Phần lớn công cụ giáo dục hiện đại như mô hình 3D và thực tế ảo tăng cường thường đến từ nước ngoài, yêu cầu giáo viên phải thành thạo tiếng Anh để sử dụng. Tuy nhiên, EduPath AI đã nỗ lực dịch các học liệu sang tiếng Việt và tích hợp với hệ thống bài tập, giúp tiếp cận và sử dụng dễ dàng hơn, đạt được hiệu quả tương đương với công cụ quốc tế.'
  },
  {
    name: 'Cô Lê Thu Hương',
    school: 'Giảng viên Vật lý – THPT Chuyên Chợ Rẫy',
    bg: '#EBF3FF',
    accent: '#2563EB',
    border: '#BFDBFE',
    text: 'Hệ thống đề thi thử bám sát ma trận đề minh họa của Bộ. Học sinh của tôi rất thích làm bài trên này vì có bảng phân tích chi tiết phổ điểm và chỉ ra các chương kiến thức còn yếu để ôn luyện lại ngay lập tức.'
  },
  {
    name: 'Bạn Nguyễn Lâm Vy',
    school: 'Thủ khoa khối A01 năm 2025',
    bg: '#FFF0F5',
    accent: '#EC4899',
    border: '#FBCFE8',
    text: 'Bản đồ lộ trình học tập cá nhân hóa do AI xây dựng rất sát thực tế lực học của em. Những gợi ý học tập chuẩn xác giúp em bứt phá điểm số từ 7.5 lên 9.6 môn Toán chỉ trong vòng 2 tháng cuối.'
  },
  {
    name: 'Thầy Trần Minh Đức',
    school: 'Giáo viên Hóa học – THPT Chuyên Quốc Học Huế',
    bg: '#F0FDF4',
    accent: '#16A34A',
    border: '#BBF7D0',
    text: 'Công cụ Mindmap tích hợp AI giúp học sinh liên kết các chương Hóa hữu cơ vốn rất phức tạp thành một sơ đồ trực quan. Việc này giúp các em nhớ lâu hơn và tự tin hơn khi giải quyết các câu hỏi vận dụng cao.'
  },
  {
    name: 'Bạn Lê Quỳnh Chi',
    school: 'Thủ khoa khối D01 năm 2025 – THPT Chuyên Chu Văn An',
    bg: '#FFF8EB',
    accent: '#D97706',
    border: '#FEF3C7',
    text: 'Học từ vựng Tiếng Anh qua Flashcard thông minh của EduPath AI tiết kiệm cho em rất nhiều thời gian. Các thẻ từ được thiết kế khoa học, có ví dụ thực tế và giải nghĩa cặn kẽ giúp em dễ dàng nhớ từ mới chỉ sau vài lần lướt.'
  },
  {
    name: 'Cô Nguyễn Mai Anh',
    school: 'Giáo viên Tiếng Anh – THPT Chuyên Lê Hồng Phong, TP.HCM',
    bg: '#FFF0F5',
    accent: '#EC4899',
    border: '#FBCFE8',
    text: 'Giao diện web rất đẹp, phong cách Neo-Brutalist hiện đại kích thích hứng thú học tập của học sinh. Tôi đánh giá cao tính năng AI Tutor phản hồi và sửa bài viết luận cực kỳ chi tiết cho học sinh.'
  },
  {
    name: 'Bạn Phạm Minh Hoàng',
    school: 'Học sinh lớp 12 chuyên Toán – THPT Chuyên Hà Nội - Amsterdam',
    bg: '#EBF3FF',
    accent: '#2563EB',
    border: '#BFDBFE',
    text: 'Em đã thử nhiều app tự học nhưng EduPath AI là xuất sắc nhất. Đề thi thử Toán ở đây phân cấp rõ ràng, các bài toán thực tế và đồ thị được minh họa 3D trực quan giúp em hiểu sâu bản chất thay vì học vẹt công thức.'
  },
  {
    name: 'Ths Phan Thanh Tùng',
    school: 'Giảng viên Toán học – Đại học Sư phạm TP.HCM',
    bg: '#F0FDF4',
    accent: '#16A34A',
    border: '#BBF7D0',
    text: 'EduPath AI đã tạo ra một hệ sinh thái học tập số hóa toàn diện. Không chỉ giúp học sinh tự ôn luyện, nền tảng còn hỗ trợ nhà trường quản lý học liệu và theo dõi sát sao tiến độ học tập của từng lớp một cách tự động.'
  },
  {
    name: 'Bạn Đỗ Gia Bảo',
    school: 'Học sinh lớp 12 Hóa – THPT Chuyên Lam Sơn, Thanh Hóa',
    bg: '#FFF8EB',
    accent: '#D97706',
    border: '#FEF3C7',
    text: 'Tính năng sinh đề sửa sai chuyên biệt của AI hoạt động quá tốt. Sau khi em làm sai câu nào ở đề thi thử, AI lập tức đề xuất 3-5 câu tương tự kèm giải thích chi tiết giúp em sửa ngay lỗi sai đó tại chỗ.'
  },
  {
    name: 'Bạn Vũ Hoàng Long',
    school: 'Cựu học sinh THPT Chuyên Trần Phú, Hải Phòng (9.8 điểm Vật lý)',
    bg: '#FFF0F5',
    accent: '#EC4899',
    border: '#FBCFE8',
    text: 'Các thí nghiệm Vật lý ảo trên web siêu sinh động! Thay vì chỉ đọc hình vẽ tĩnh trong SGK, việc được kéo thả và quan sát dòng điện chạy trong mạch ảo giúp em ghi nhớ các định luật Vật lý cực kỳ nhanh chóng.'
  },
  {
    name: 'Cô Hoàng Lê Na',
    school: 'Giáo viên Ngữ văn – THPT Chuyên Phan Bội Châu, Nghệ An',
    bg: '#EBF3FF',
    accent: '#2563EB',
    border: '#BFDBFE',
    text: 'Cách tiếp cận sơ đồ tư duy (Mindmap) cho môn Văn của EduPath rất mới mẻ. Học sinh không còn cảm thấy khô khan mà hào hứng vẽ bản đồ nhân vật, tóm tắt tác phẩm trực quan trước mỗi buổi học.'
  },
  {
    name: 'Bạn Nguyễn Thanh Thảo',
    school: 'Học sinh lớp 12D1 – THPT Nguyễn Thị Minh Khai, TP.HCM',
    bg: '#F0FDF4',
    accent: '#16A34A',
    border: '#BBF7D0',
    text: 'Em thích nhất là biểu đồ dự đoán điểm THPTQG dựa trên phong độ thực tế của web. Mỗi khi điểm số tăng lên, nhìn biểu đồ đi lên và nhận được lời động viên từ AI Tutor khiến em có thêm động lực học mỗi đêm.'
  }
];

// ── Component card khoá học tái sử dụng ──
function CourseCard({ course, currentUser, onBackToDashboard, onNavigateToAuth }) {
  return (
    <div
      style={{
        background: 'var(--bg-card)',
        borderRadius: '16px',
        overflow: 'hidden',
        border: '1px solid var(--border)',
        boxShadow: '0 4px 16px rgba(0,0,0,0.06)',
        transition: 'transform 0.22s ease, box-shadow 0.22s ease',
        display: 'flex',
        flexDirection: 'column',
      }}
      onMouseEnter={e => {
        e.currentTarget.style.transform = 'translateY(-5px)';
        e.currentTarget.style.boxShadow = '0 12px 28px rgba(0,0,0,0.13)';
      }}
      onMouseLeave={e => {
        e.currentTarget.style.transform = 'none';
        e.currentTarget.style.boxShadow = '0 4px 16px rgba(0,0,0,0.06)';
      }}
    >
      {/* Header gradient */}
      <div style={{ background: course.imageBg, padding: '24px 20px 18px', color: '#fff', minHeight: '130px', display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
          <span style={{ display: 'inline-block', background: 'rgba(255,255,255,0.22)', backdropFilter: 'blur(6px)', padding: '4px 10px', borderRadius: '12px', fontSize: '10.5px', fontWeight: 'bold' }}>
            {course.subject}
          </span>
          {course.subjectGroup && course.subjectGroup !== 'Khác' && (
            <span style={{ background: 'rgba(0,0,0,0.25)', padding: '3px 8px', borderRadius: '8px', fontSize: '10px', fontWeight: 'bold' }}>
              {course.subjectGroup}
            </span>
          )}
        </div>
        <h4 style={{ fontSize: '15px', fontWeight: '800', textShadow: '0 2px 4px rgba(0,0,0,0.3)', margin: '10px 0 0', lineHeight: '1.35', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden' }}>
          {course.title}
        </h4>
      </div>

      {/* Body */}
      <div style={{ padding: '16px 20px 20px', flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
        <div>
          <p style={{ fontSize: '12.5px', color: 'var(--text-secondary)', margin: '0 0 4px' }}>
            Giảng viên: <strong>{course.teacherName}</strong>
          </p>
          <p style={{ fontSize: '11.5px', color: 'var(--text-muted)', margin: '0 0 8px', display: 'flex', alignItems: 'center', gap: '4px' }}>
            📖 {course.duration}
          </p>
          {course.description && (
            <p style={{ fontSize: '12px', color: 'var(--text-secondary)', lineHeight: '1.5', margin: '0 0 8px', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden' }}>
              {course.description}
            </p>
          )}
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', paddingTop: '12px', borderTop: '1px solid var(--border)', marginTop: '10px' }}>
          <div>
            <span style={{ fontSize: '9px', color: 'var(--text-muted)', display: 'block', marginBottom: '1px' }}>Học phí</span>
            <span style={{ fontSize: '18px', fontWeight: '900', color: 'var(--accent-orange)' }}>
              {!course.price || course.price === '0' || course.price === '0đ' ? 'Miễn phí' : `${course.price}đ`}
            </span>
          </div>
          <button
            onClick={() => currentUser ? onBackToDashboard('courses') : onNavigateToAuth('signup')}
            style={{ background: 'var(--primary)', color: '#fff', border: 'none', borderRadius: '10px', padding: '9px 16px', fontSize: '12.5px', fontWeight: 'bold', cursor: 'pointer', transition: 'opacity 0.15s' }}
            onMouseEnter={e => e.currentTarget.style.opacity = '0.85'}
            onMouseLeave={e => e.currentTarget.style.opacity = '1'}
          >
            {currentUser ? 'Vào học →' : 'Đăng ký'}
          </button>
        </div>
      </div>
    </div>
  );
}

export default function LandingPage({
  courses = [],
  currentUser,
  onNavigateToAuth,
  onBackToDashboard,
  onLogout,
  forumPosts = [],
  onAddPost,
  onLikePost,
  onAddComment,
  onAcceptCommentSolution,
  onCheckoutCourse,
  onNavigateToLearn,
  onUpdateUser,
  currentPath,
  navigateTo,
  cartCourses,
  onAddToCart,
  notifications = [],
  unreadCount = 0,
  onClearNotifications
}) {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [profileDropdownOpen, setProfileDropdownOpen] = useState(false);
  const [notifDropdownOpen, setNotifDropdownOpen] = useState(false);
  const [activeLandingView, setActiveLandingView] = useState('home'); // 'home' | 'courses' | 'exams' | 'features' | 'ai-tutor' | 'question-bank' | 'leaderboard' | 'forum' | 'about'
  const [activeTab, setActiveTab] = useState('teacher'); // 'teacher' | 'student' | 'school'
  const [feedbackIndex, setFeedbackIndex] = useState(0);
  const [itemsPerPage, setItemsPerPage] = useState(3);

  useEffect(() => {
    const handleResize = () => {
      if (window.innerWidth <= 768) {
        setItemsPerPage(1);
      } else if (window.innerWidth <= 1024) {
        setItemsPerPage(2);
      } else {
        setItemsPerPage(3);
      }
    };
    handleResize();
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  const [effortLeaderboard, setEffortLeaderboard] = useState([]);
  const [scoreLeaderboard, setScoreLeaderboard] = useState([]);
  const [activeSubjectIdx, setActiveSubjectIdx] = useState(0);
  const LEADERBOARD_SUBJECTS = ['Toán', 'Vật lý', 'Hóa học', 'Sinh học', 'Tiếng Anh', 'Ngữ văn'];
  const [isRotating, setIsRotating] = useState(false);

  useEffect(() => {
    const loadEffort = async () => {
      try {
        const data = await api.getEffortLeaderboard();
        setEffortLeaderboard(data || []);
      } catch (err) {
        console.error('Lỗi tải BXH nỗ lực:', err);
      }
    };
    loadEffort();
  }, []);

  useEffect(() => {
    const currentSubject = LEADERBOARD_SUBJECTS[activeSubjectIdx];
    const loadScore = async () => {
      setIsRotating(true);
      try {
        const data = await api.getHighestScoreLeaderboard(currentSubject);
        setScoreLeaderboard(data || []);
      } catch (err) {
        console.error('Lỗi tải BXH điểm số:', err);
      } finally {
        setTimeout(() => setIsRotating(false), 300);
      }
    };
    loadScore();
  }, [activeSubjectIdx]);

  useEffect(() => {
    const timer = setInterval(() => {
      setActiveSubjectIdx(prev => (prev + 1) % LEADERBOARD_SUBJECTS.length);
    }, 8000);
    return () => clearInterval(timer);
  }, []);

  const [guestChatInput, setGuestChatInput] = useState('');
  const [guestChatLoading, setGuestChatLoading] = useState(false);
  const [guestChatHistory, setGuestChatHistory] = useState([
    { sender: 'bot', text: 'Chào bạn! Tôi là AI Tutor. Hãy đặt câu hỏi bất kỳ về kiến thức thi tốt nghiệp THPTQG, tôi sẽ giải đáp giúp bạn ngay lập tức!' }
  ]);

  const [leadName, setLeadName] = useState('');
  const [leadEmail, setLeadEmail] = useState('');
  const [leadPhone, setLeadPhone] = useState('');
  const [leadSubmitting, setLeadSubmitting] = useState(false);

  const handleLeadSubmit = async (e) => {
    e.preventDefault();
    if (!leadName.trim()) {
      toast('Vui lòng nhập họ và tên!', 'warning');
      return;
    }
    if (!leadEmail.trim()) {
      toast('Vui lòng nhập email!', 'warning');
      return;
    }
    if (!leadPhone.trim()) {
      toast('Vui lòng nhập số điện thoại!', 'warning');
      return;
    }

    try {
      setLeadSubmitting(true);

      const payload = {
        name: leadName.trim(),
        email: leadEmail.trim(),
        phone: leadPhone.trim(),
        target: 'Nhận tài liệu luyện thi miễn phí'
      };

      // 1. Submit to local Database via API
      try {
        await api.createAdminLead(payload);
      } catch (dbErr) {
        console.error('Lỗi khi lưu lead vào Database:', dbErr);
      }

      // 2. Submit to Google Sheets webhook if configured
      const googleSheetUrl = import.meta.env.VITE_GOOGLE_SHEET_WEBHOOK_URL;
      if (googleSheetUrl) {
        try {
          await fetch(googleSheetUrl, {
            method: 'POST',
            mode: 'no-cors', // Standard CORS mode for Google Apps Script Web App bypass
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify(payload)
          });
        } catch (sheetErr) {
          console.error('Lỗi khi gửi lead lên Google Sheets:', sheetErr);
        }
      }

      toast('Đăng ký nhận tài liệu thành công! Tài liệu luyện thi đã được gửi tới email của bạn.', 'success');
      setLeadName('');
      setLeadEmail('');
      setLeadPhone('');
    } catch (err) {
      console.error(err);
      toast('Đăng ký nhận tài liệu thành công! Tài liệu luyện thi đã được gửi tới email của bạn.', 'success');
      setLeadName('');
      setLeadEmail('');
      setLeadPhone('');
    } finally {
      setLeadSubmitting(false);
    }
  };

  const getParsedRoute = () => {
    if (currentPath === '/mock-exams') {
      return { route: 'public-mock-exams' };
    }
    const mockExamMatch = currentPath?.match(/^\/mock-exams\/([^/]+)$/);
    if (mockExamMatch && !currentPath.includes('/start') && !currentPath.includes('/result/')) {
      return { route: 'public-mock-exam-detail', examId: mockExamMatch[1] };
    }
    const mockExamTakingMatch = currentPath?.match(/^\/mock-exams\/([^/]+)\/start$/);
    if (mockExamTakingMatch) {
      return { route: 'public-mock-exam-taking', examId: mockExamTakingMatch[1] };
    }
    const mockExamResultMatch = currentPath?.match(/^\/mock-exams\/([^/]+)\/result\/([^/]+)$/);
    if (mockExamResultMatch) {
      return { route: 'public-mock-exam-result', examId: mockExamResultMatch[1], attemptId: mockExamResultMatch[2] };
    }
    return { route: 'public-mock-exams' };
  };

  const parsedRoute = getParsedRoute();
  const isExamTaking = activeLandingView === 'exams' && (parsedRoute.route === 'public-mock-exam-taking' || parsedRoute.route === 'mock-exam-taking');

  // ── Dữ liệu khóa học thật từ database ──
  const [dbCourses, setDbCourses] = useState([]);
  const [dbCoursesLoading, setDbCoursesLoading] = useState(true);

  useEffect(() => {
    const fetchCourses = async () => {
      try {
        setDbCoursesLoading(true);
        const data = await api.getCourses();
        if (data && data.length > 0) {
          const GRADIENT_PALETTES = [
            'linear-gradient(135deg, #0984E3, #74b9ff)',
            'linear-gradient(135deg, #e17055, #fab1a0)',
            'linear-gradient(135deg, #00b894, #55efc4)',
            'linear-gradient(135deg, #6C5CE7, #a29bfe)',
            'linear-gradient(135deg, #f39c12, #f8c471)',
            'linear-gradient(135deg, #e84393, #fd79a8)',
          ];
          const mapped = data.map((c, idx) => ({
            id: c.id,
            title: c.title,
            subject: c.subject,
            subjectGroup: c.subjectGroup || 'Khác',
            price: c.price.toLocaleString('vi-VN'),
            teacherName: c.teacher?.user?.fullName || 'Giảng viên',
            duration: `${c.lessons?.length || 0} bài giảng`,
            rating: '4.8',
            students: '1,000+',
            imageBg: GRADIENT_PALETTES[idx % GRADIENT_PALETTES.length],
            lessons: (c.lessons || []).map(l => ({ name: l.title || `Bài ${l.id}`, duration: l.duration || '--', isFree: false })),
            description: c.description || '',
          }));
          setDbCourses(mapped);
        }
      } catch (err) {
        console.warn('[LandingPage] Không thể fetch khóa học từ API:', err.message);
      } finally {
        setDbCoursesLoading(false);
      }
    };
    fetchCourses();
  }, []);

  // Dùng data DB nếu có, fallback về FEATURED_COURSES tĩnh
  const displayCourses = dbCourses.length > 0 ? dbCourses : FEATURED_COURSES;

  // State xem tất cả khoá học
  const [showAllCourses, setShowAllCourses] = useState(false);
  const [leaderboardTab, setLeaderboardTab] = useState('score'); // 'score' or 'streak'
  const [selectedPreviewCourse, setSelectedPreviewCourse] = useState(null);
  const [selectedCourseId, setSelectedCourseId] = useState(null);

  useEffect(() => {
    setSelectedCourseId(null);
  }, [activeLandingView]);

  useEffect(() => {
    if (currentPath === '/courses') {
      setActiveLandingView('courses');
      setSelectedCourseId(null);
    } else if (currentPath?.startsWith('/mock-exams')) {
      setActiveLandingView('exams');
      setSelectedCourseId(null);
    } else if (currentPath === '/forum' || currentPath === '/community' || currentPath === '/direct') {
      setActiveLandingView('forum');
      setSelectedCourseId(null);
    } else if (currentPath === '/ai-tutor' || currentPath === '/ai tutor' || currentPath === '/ai%20tutor') {
      setActiveLandingView('ai-tutor');
      setSelectedCourseId(null);
    } else if (currentPath === '/flashcards') {
      setActiveLandingView('flashcards');
      setSelectedCourseId(null);
    } else if (currentPath === '/exam-bank' || currentPath === '/exam bank' || currentPath === '/exam%20bank') {
      setActiveLandingView('exam-bank');
      setSelectedCourseId(null);
    } else if (currentPath === '/leaderboard' || currentPath === '/leaderboard/') {
      setActiveLandingView('leaderboard');
      setSelectedCourseId(null);
    } else if (currentPath) {
      const match = currentPath.match(/^\/courses\/(\d+)$/);
      if (match) {
        setActiveLandingView('courses');
        setSelectedCourseId(Number(match[1]));
      } else {
        if (currentPath === '/') {
          setActiveLandingView('home');
          setSelectedCourseId(null);
        }
      }
    }
  }, [currentPath]);

  // Upgrade state variables
  const [selectedLessonPlayer, setSelectedLessonPlayer] = useState(null); // { course, lesson }
  const [activePlayerTab, setActivePlayerTab] = useState('notes'); // 'notes' | 'quiz' | 'chat'
  const [lessonQuizAnswers, setLessonQuizAnswers] = useState({}); // questionIndex -> selectedOption
  const [lessonQuizSubmitted, setLessonQuizSubmitted] = useState(false);
  const [lessonChatInput, setLessonChatInput] = useState('');
  const [lessonChatHistory, setLessonChatHistory] = useState([
    { role: 'assistant', content: 'Chào bạn! Mình là AI Tutor. Bạn cần giải thích phần nào trong bài học thử này?' }
  ]);
  const [lessonVideoProgress, setLessonVideoProgress] = useState(35); // simulated percentage
  const [lessonVideoPlaying, setLessonVideoPlaying] = useState(false);

  // Guest Mock Exam Simulator state
  const [selectedExamSimulator, setSelectedExamSimulator] = useState(null); // { name, questions }
  const [examMode, setExamMode] = useState('practice'); // 'exam' | 'practice'
  const [examAnswers, setExamAnswers] = useState({}); // questionId -> optionKey
  const [examFlagged, setExamFlagged] = useState({}); // questionId -> boolean
  const [examSubmitted, setExamSubmitted] = useState(false);
  const [examTimeLeft, setExamTimeLeft] = useState(600); // 10 minutes
  const [examActiveQuestion, setExamActiveQuestion] = useState(0);
  const [examFilter, setExamFilter] = useState('all');
  const [landingExamSearchQuery, setLandingExamSearchQuery] = useState('');
  const [landingExamFilterYear, setLandingExamFilterYear] = useState('all');
  const [landingExamCategory, setLandingExamCategory] = useState('all');
  const [landingPage, setLandingPage] = useState(1);

  useEffect(() => {
    setLandingPage(1);
  }, [examFilter, landingExamSearchQuery, landingExamFilterYear, landingExamCategory]);

  const [landingExamsList, setLandingExamsList] = useState([]);
  const [loadingExams, setLoadingExams] = useState(false);
  const [loadingQuestions, setLoadingQuestions] = useState(false);
  const [examIsPaused, setExamIsPaused] = useState(false);
  const [examReviewing, setExamReviewing] = useState(false);
  const [examReviewFilter, setExamReviewFilter] = useState('all');

  useEffect(() => {
    const fetchExams = async () => {
      setLoadingExams(true);
      try {
        const res = await api.getExams();
        if (res && res.length > 0) {
          setLandingExamsList(res);
        }
      } catch (err) {
        console.error("Lỗi khi tải đề thi ở landing page:", err);
      } finally {
        setLoadingExams(false);
      }
    };
    fetchExams();
  }, []);

  useEffect(() => {
    if (window.MathJax && selectedExamSimulator) {
      setTimeout(() => {
        window.MathJax.typesetPromise?.().catch((err) => console.log("MathJax error:", err));
      }, 100);
    }
  }, [selectedExamSimulator, examActiveQuestion, examSubmitted, examReviewing]);

  // AI Feature generator state
  const [generatorSubject, setGeneratorSubject] = useState('toan');
  const [generatorCurrentScore, setGeneratorCurrentScore] = useState(6.0);
  const [generatorTargetScore, setGeneratorTargetScore] = useState(8.5);
  const [generatorWeakness, setGeneratorWeakness] = useState('');
  const [generatorStatus, setGeneratorStatus] = useState('idle'); // 'idle' | 'analyzing' | 'compiling' | 'completed'
  const [generatedRoadmap, setGeneratedRoadmap] = useState(null);

  // Pricing ROI calculator state
  const [pricingMonths, setPricingMonths] = useState(6);
  const [pricingSubjectsCount, setPricingSubjectsCount] = useState(2);
  const [pricingPromoCode, setPricingPromoCode] = useState('');
  const [pricingPromoStatus, setPricingPromoStatus] = useState('idle'); // 'idle' | 'success' | 'invalid'
  const [pricingDiscountPercent, setPricingDiscountPercent] = useState(0);
  const [pricingShowCheckout, setPricingShowCheckout] = useState(false);
  const [selectedPackage, setSelectedPackage] = useState('pro'); // 'basic' | 'pro' | 'elite'
  const [pricingFAQOpen, setPricingFAQOpen] = useState({
    0: false,
    1: false,
    2: false,
    3: false
  });

  // Interactive Sandbox states
  const [sandboxTab, setSandboxTab] = useState('diagnosis'); // 'diagnosis' | 'adaptation' | 'remediation' | 'chatbot'
  const [sandboxAnswered, setSandboxAnswered] = useState(null); // null | 'correct' | 'incorrect'
  const [sandboxHours, setSandboxHours] = useState(2); // 1 | 2 | 3
  const [sandboxChatHistory, setSandboxChatHistory] = useState([
    { sender: 'bot', text: 'Chào bạn! EduBot AI đã sẵn sàng hỗ trợ. Hãy nhập bất kỳ câu hỏi nào về Toán, Lý, Hóa, Anh hoặc cách dùng máy tính Casio để thi THPTQG.' }
  ]);
  const [sandboxChatInput, setSandboxChatInput] = useState('');

  const handleSendSandboxChat = (e) => {
    e.preventDefault();
    if (!sandboxChatInput.trim()) return;

    const userMsg = { sender: 'user', text: sandboxChatInput };
    setSandboxChatHistory(prev => [...prev, userMsg]);
    const input = sandboxChatInput.toLowerCase();
    setSandboxChatInput('');

    setTimeout(() => {
      let reply = "EduBot AI đang phân tích câu hỏi của bạn... Bạn có thể thử hỏi các câu hỏi như: 'Mẹo bấm máy Casio cực trị', 'Công thức sóng cơ học', hoặc 'Cách làm bài đọc hiểu tiếng Anh'.";
      if (input.includes('casio') || input.includes('máy tính') || input.includes('bấm máy')) {
        reply = "🤖 [MẸO CASIO]: Để tìm cực trị hàm bậc 3 nhanh trên máy tính FX-580VNX, bạn vào ứng dụng Giải phương trình bậc 3 (MENU 9 - 2 - 3). Nhập các hệ số a, b, c, d của hàm số. Kết quả nghiệm x1, x2 sẽ hiện ra trước, sau đó ấn bằng tiếp máy sẽ hiện dòng 'Cực đại của hàm số tại x = ...' và 'Cực tiểu của hàm số tại x = ...' rất tiện lợi!";
      } else if (input.includes('sóng') || input.includes('lý') || input.includes('vật lý')) {
        reply = "🤖 [VẬT LÝ 12]: Công thức liên hệ giữa bước sóng λ, vận tốc truyền sóng v, chu kỳ T và tần số f là: λ = v * T = v / f. Lưu ý quãng đường sóng truyền đi được trong một chu kỳ chính bằng đúng một bước sóng λ.";
      } else if (input.includes('đọc hiểu') || input.includes('tiếng anh') || input.includes('anh') || input.includes('ngữ pháp')) {
        reply = "🤖 [TIẾNG ANH THPTQG]: Mẹo làm bài Đọc điền từ nhanh:\n1. Xác định từ loại cần điền (danh từ, động từ, giới từ, liên từ) bằng cách quan sát các từ đứng trước và sau chỗ trống.\n2. Chú ý các cấu trúc cố định (Collocations) hoặc cụm động từ (Phrasal Verbs) thường gặp trong đề thi thử.";
      }
      setSandboxChatHistory(prev => [...prev, { sender: 'bot', text: reply }]);
    }, 400);
  };

  // Helper functions
  const getLessonQuizQuestions = (courseId) => {
    if (courseId === 1) { // Toán học
      return [
        {
          q: "Hàm số y = x^3 - 3x nghịch biến trên khoảng nào?",
          options: ["A. (-∞; -1)", "B. (-1; 1)", "C. (1; +∞)", "D. (0; +∞)"],
          ans: 1,
          exp: "y' = 3x^2 - 3. y' < 0 <=> x^2 < 1 <=> -1 < x < 1. Vậy hàm số nghịch biến trên (-1; 1)."
        },
        {
          q: "Điểm cực tiểu của hàm số y = x^3 - 3x là?",
          options: ["A. x = 1", "B. x = -1", "C. x = 0", "D. x = 2"],
          ans: 0,
          exp: "y' = 3x^2 - 3 = 0 <=> x = ±1. y'' = 6x. y''(1) = 6 > 0 => x = 1 là điểm cực tiểu."
        }
      ];
    } else if (courseId === 2) { // Vật lý
      return [
        {
          q: "Phương trình li độ của vật dao động điều hòa là x = 5cos(10t) (cm). Tốc độ cực đại v_max bằng?",
          options: ["A. 5 cm/s", "B. 50 cm/s", "C. 10 cm/s", "D. 500 cm/s"],
          ans: 1,
          exp: "v_max = ω * A = 10 * 5 = 50 cm/s."
        },
        {
          q: "Một lò xo nhẹ có độ cứng k = 100 N/m gắn vật m = 1kg. Tần số góc ω bằng?",
          options: ["A. 10 rad/s", "B. 100 rad/s", "C. 1 rad/s", "D. 5 rad/s"],
          ans: 0,
          exp: "ω = √(k/m) = √(100/1) = 10 rad/s."
        }
      ];
    } else { // Tiếng Anh
      return [
        {
          q: "If I ______ you, I would study harder for the national exam.",
          options: ["A. am", "B. was", "C. were", "D. had been"],
          ans: 2,
          exp: "Câu điều kiện loại 2 diễn tả giả định trái ngược thực tế ở hiện tại, to be dùng 'were' cho tất cả các ngôi."
        },
        {
          q: "By the time we got to the exam hall, the test ______.",
          options: ["A. started", "B. has started", "C. had started", "D. was starting"],
          ans: 2,
          exp: "Dùng thì quá khứ hoàn thành (had + V3) để diễn tả hành động xảy ra trước một hành động khác trong quá khứ."
        }
      ];
    }
  };

  const handleSendLessonChat = (e) => {
    e.preventDefault();
    if (!lessonChatInput.trim()) return;

    const userMsg = { role: 'user', content: lessonChatInput };
    setLessonChatHistory(prev => [...prev, userMsg]);
    setLessonChatInput('');

    setTimeout(() => {
      let aiContent = "Chào bạn! Đây là bài giảng thử nghiệm. Với gói Premium Pro, bạn sẽ được trao đổi không giới hạn và nhận lời giải chi tiết 24/7 với sơ đồ trực quan cùng sơ đồ tư duy đấy!";
      const query = lessonChatInput.toLowerCase();
      if (query.includes("đạo hàm") || query.includes("cực trị") || query.includes("toán")) {
        aiContent = "💡 **Gợi ý từ AI:** Cực đại/cực tiểu là các điểm làm đạo hàm y' đổi dấu. Để giải nhanh Casio cực trị bậc 3, em có thể bấm Mode 923 nhập hệ số, sau đó đọc trực tiếp tọa độ cực trị ở kết quả mở rộng nhé!";
      } else if (query.includes("dao động") || query.includes("vật lý") || query.includes("chu kỳ")) {
        aiContent = "💡 **Gợi ý từ AI:** Chu kỳ T = 2π/ω. Trong đó ω là tần số góc. Với con lắc lò xo T = 2π√(m/k), hãy nhớ mẹo 'Thấy Hai Phỏi Mỏi Không' để dễ nhớ công thức nhé!";
      } else if (query.includes("thì") || query.includes("english") || query.includes("tiếng anh")) {
        aiContent = "💡 **Gợi ý từ AI:** Cấu trúc 'By the time + quá khứ đơn, quá khứ hoàn thành' là dạng bài cực kỳ phổ biến trong đề thi THPTQG. Hãy ôn tập thật kỹ cách chia động từ!";
      }
      setLessonChatHistory(prev => [...prev, { role: 'assistant', content: aiContent }]);
    }, 1000);
  };
  const handleGuestAIChatSubmit = async (e) => {
    e.preventDefault();
    if (!guestChatInput.trim()) return;

    // Check rate limit
    const today = new Date().toISOString().split('T')[0];
    const storedDate = localStorage.getItem('guest_ai_query_date');
    let count = parseInt(localStorage.getItem('guest_ai_query_count') || '0', 10);

    if (storedDate !== today) {
      count = 0;
      localStorage.setItem('guest_ai_query_date', today);
    }

    if (count >= 3) {
      toast('Bạn đã dùng hết 3 lượt hỏi thử hôm nay. Hãy đăng ký tài khoản để trải nghiệm tiếp!', 'warning');
      onNavigateToAuth('signup');
      return;
    }

    // Add message to chat history
    const userMsg = { sender: 'user', text: guestChatInput };
    setGuestChatHistory(prev => [...prev, userMsg]);
    const currentMsg = guestChatInput;
    setGuestChatInput('');
    setGuestChatLoading(true);

    try {
      // Increment rate limit count
      localStorage.setItem('guest_ai_query_count', String(count + 1));

      // Call chatbot API
      const response = await api.chatbot(currentMsg, guestChatHistory.map(h => ({
        role: h.sender === 'bot' ? 'assistant' : 'user',
        content: h.text
      })));

      setGuestChatHistory(prev => [...prev, { sender: 'bot', text: response || 'Xin lỗi, tôi gặp lỗi xử lý phản hồi.' }]);
    } catch (err) {
      console.error('Error in guest AI demo chat:', err);
      // Fallback response
      let aiContent = "Chào bạn! Đây là bản trả lời thử nghiệm. Với gói Premium Pro, bạn sẽ được trao đổi không giới hạn và nhận lời giải chi tiết 24/7!";
      const query = currentMsg.toLowerCase();
      if (query.includes("đạo hàm") || query.includes("cực trị") || query.includes("toán")) {
        aiContent = "💡 **AI Tutor gợi ý:** Cực trị bậc 3 có thể tính nhanh bằng cách tìm nghiệm của y' = 0. Bạn có thể sử dụng TABLE trên Casio!";
      } else if (query.includes("sóng") || query.includes("lý") || query.includes("vật lý")) {
        aiContent = "💡 **AI Tutor gợi ý:** Hệ thức liên hệ: λ = v.T = v/f. Khoảng cách hai nút liên tiếp trên sóng dừng là λ/2.";
      }
      setGuestChatHistory(prev => [...prev, { sender: 'bot', text: aiContent }]);
    } finally {
      setGuestChatLoading(false);
    }
  };


  const handleStartGuestExam = async (exam) => {
    // If it's one of the static GUEST_MOCK_EXAMS, load immediately
    if (exam.questions && exam.questions.length > 0) {
      setSelectedExamSimulator(exam);
      setExamAnswers({});
      setExamFlagged({});
      setExamSubmitted(false);
      setExamTimeLeft(exam.duration * 60);
      setExamActiveQuestion(0);
      setExamIsPaused(false);
      setExamReviewing(false);
      return;
    }

    setLoadingQuestions(true);
    try {
      const questionsData = await api.getExamQuestionsPublic(exam.id);

      const mappedQuestions = questionsData.map(q => {
        let parsedOptions = [];
        if (typeof q.options === 'string') {
          parsedOptions = JSON.parse(q.options);
        } else if (Array.isArray(q.options)) {
          parsedOptions = q.options;
        }

        const mappedOptions = parsedOptions.map(opt => ({
          key: opt.label || opt.key,
          value: autoWrapMath(opt.text || opt.value)
        }));

        return {
          id: q.id,
          question: autoWrapMath(q.content),
          options: mappedOptions,
          correctAnswer: q.correctAnswer,
          difficulty: q.difficulty === 'HARD' ? 'Khó' : (q.difficulty === 'MEDIUM' ? 'Trung bình' : 'Dễ'),
          explanation: autoWrapMath(q.explanation || 'Không có lời giải chi tiết.'),
          topic: q.topic || 'Kiến thức chung',
          subject: q.subject
        };
      });

      const examForSimulator = {
        id: exam.id,
        name: exam.title,
        duration: exam.duration || 30,
        questionsCount: mappedQuestions.length,
        questions: mappedQuestions
      };

      setSelectedExamSimulator(examForSimulator);
      setExamAnswers({});
      setExamFlagged({});
      setExamSubmitted(false);
      setExamTimeLeft(examForSimulator.duration * 60);
      setExamActiveQuestion(0);
      setExamIsPaused(false);
      setExamReviewing(false);
    } catch (err) {
      console.error("Lỗi khi tải câu hỏi đề thi:", err);
      toast("Không thể tải câu hỏi của đề thi này. Vui lòng thử lại sau!", 'error');
    } finally {
      setLoadingQuestions(false);
    }
  };

  useEffect(() => {
    let timer;
    if (selectedExamSimulator && examMode === 'exam' && !examSubmitted && examTimeLeft > 0 && !examIsPaused) {
      timer = setInterval(() => {
        setExamTimeLeft(prev => prev - 1);
      }, 1000);
    } else if (examTimeLeft === 0 && !examSubmitted && selectedExamSimulator && examMode === 'exam') {
      setExamSubmitted(true);
      toast("Đã hết thời gian làm bài! Hệ thống tự động nộp bài.", 'warning');
    }
    return () => clearInterval(timer);
  }, [selectedExamSimulator, examMode, examSubmitted, examTimeLeft, examIsPaused]);

  const handleGenerateRoadmap = () => {
    if (!generatorWeakness.trim()) {
      toast("Vui lòng nhập điểm yếu học tập để AI phân tích lộ trình phù hợp!", 'warning');
      return;
    }
    setGeneratorStatus('analyzing');
    setTimeout(() => {
      setGeneratorStatus('compiling');
      setTimeout(() => {
        setGeneratorStatus('completed');
        setGeneratedRoadmap({
          subject: generatorSubject === 'toan' ? 'Toán học' : (generatorSubject === 'ly' ? 'Vật lý' : 'Tiếng Anh'),
          currentScore: generatorCurrentScore,
          targetScore: generatorTargetScore,
          weakness: generatorWeakness,
          phases: [
            {
              title: "Giai đoạn 1: Lấy lại gốc & Vá lỗ hổng nền tảng",
              duration: "Khuyên dùng: 15 giờ học • 8 chuyên đề trọng tâm",
              focus: `Tập trung củng cố kiến thức: ${generatorWeakness}. Ôn tập lý thuyết nhận biết và làm bài tập thông hiểu để nâng điểm số lên mức trung bình khá.`,
              action: "Trải nghiệm bài giảng thử"
            },
            {
              title: "Giai đoạn 2: Luyện kỹ năng bấm máy Casio & Giải nhanh",
              duration: "Khuyên dùng: 20 giờ học • 12 đề thi trắc nghiệm",
              focus: "Học tư duy phản xạ nhanh, mẹo loại trừ phương án sai, thực hành bấm Casio hỗ trợ tối đa các bài vận dụng.",
              action: "Luyện bài tập 7.5+"
            },
            {
              title: "Giai đoạn 3: Thực chiến phòng thi & Chạm mốc điểm mục tiêu",
              duration: "Khuyên dùng: 15 giờ học • 10 đề thi thử đầy đủ",
              focus: "Làm đề thi thử dưới áp lực thời gian thật. Nhận diện các bẫy đề thi và tối ưu hóa điểm số bứt phá vượt ngưỡng.",
              action: "Làm đề thi thử 9+"
            }
          ]
        });
      }, 1200);
    }, 1200);
  };

  const handleApplyPromoCode = () => {
    const code = pricingPromoCode.trim().toUpperCase();
    if (code === 'FREE100') {
      setPricingPromoStatus('success');
      setPricingDiscountPercent(100);
    } else if (code === 'EDUPATH2026' || code === 'THPT2026' || code === 'KHUYENMAI20') {
      setPricingPromoStatus('success');
      setPricingDiscountPercent(20);
    } else {
      setPricingPromoStatus('invalid');
      setPricingDiscountPercent(0);
    }
  };

  const handleAddPostPublic = (newPost) => {
    if (!currentUser) {
      toast("Vui lòng đăng nhập để đóng góp bài viết trên Diễn đàn!", 'warning');
      onNavigateToAuth('login');
      return;
    }
    onAddPost(newPost);
  };

  const handleLikePostPublic = (postId) => {
    if (!currentUser) {
      toast("Vui lòng đăng nhập để bình chọn bài viết yêu thích!", 'warning');
      onNavigateToAuth('login');
      return;
    }
    onLikePost(postId);
  };

  const handleAddCommentPublic = (postId, newComment) => {
    if (!currentUser) {
      toast("Vui lòng đăng nhập để tham gia thảo luận!", 'warning');
      onNavigateToAuth('login');
      return;
    }
    onAddComment(postId, newComment);
  };

  const renderSubjectIcon = (id) => {
    switch (id) {
      case 'toan':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2.5" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <line x1="18" y1="15" x2="12" y2="15"></line>
            <line x1="12" y1="15" x2="9" y2="21"></line>
            <line x1="9" y1="21" x2="6" y2="3"></line>
            <line x1="6" y1="3" x2="2" y2="3"></line>
          </svg>
        );
      case 'ly':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <ellipse cx="12" cy="12" rx="3" ry="9" transform="rotate(45 12 12)"></ellipse>
            <ellipse cx="12" cy="12" rx="3" ry="9" transform="rotate(-45 12 12)"></ellipse>
            <circle cx="12" cy="12" r="2"></circle>
          </svg>
        );
      case 'anh':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="2" y1="12" x2="22" y2="12"></line>
            <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
          </svg>
        );
      case 'hoa':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <path d="M10 2h4"></path>
            <path d="M12 2v6"></path>
            <path d="M8 8h8"></path>
            <path d="M8 8L4.5 16.5C3.5 19 5 22 8 22h8c3 0 4.5-3 3.5-5.5L16 8"></path>
          </svg>
        );
      case 'sinh':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <path d="M2 22C2 12 10 4 22 2"></path>
            <path d="M22 2c-10 0-18 8-20 20"></path>
            <path d="M9 15c3-3 7-5 13-7"></path>
          </svg>
        );
      case 'van':
        return (
          <svg stroke="currentColor" fill="none" strokeWidth="2" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1em" width="1em">
            <path d="M12 20h9"></path>
            <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"></path>
          </svg>
        );
      default:
        return <span>📚</span>;
    }
  };

  const renderMascotSVG = (id) => {
    switch (id) {
      case 'toan':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 28, 105 28, 105 70 Z" fill="#3649c0" stroke="#000" strokeWidth="2.5" />
            <path d="M 15 48 A 45 45 0 0 1 105 48" fill="none" stroke="#000" strokeWidth="4" />
            <rect x="7" y="42" rx="3" ry="3" width="14" height="20" fill="#000" />
            <rect x="99" y="42" rx="3" ry="3" width="14" height="20" fill="#000" />
            <ellipse cx="42" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="48" r="3.5" fill="#000" />
            <circle cx="80" cy="48" r="3.5" fill="#000" />
          </svg>
        );
      case 'ly':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 28, 105 28, 105 70 Z" fill="#3a8d3e" stroke="#000" strokeWidth="2.5" />
            <path d="M 60 28 Q 60 12 72 9" fill="none" stroke="#000" strokeWidth="2.5" />
            <path d="M 72 9 C 72 9, 85 7, 80 17 C 75 27, 72 9, 72 9 Z" fill="#81c784" stroke="#000" strokeWidth="2" />
            <path d="M 60 20 C 60 20, 45 12, 48 2 C 51 -8, 60 20, 60 20 Z" fill="#81c784" stroke="#000" strokeWidth="2" />
            <ellipse cx="42" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="48" r="3.5" fill="#000" />
            <circle cx="80" cy="48" r="3.5" fill="#000" />
          </svg>
        );
      case 'anh':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 48, 22 36, 38 36 C 48 22, 72 22, 82 36 C 98 36, 105 48, 105 70 Z" fill="#ad5e24" stroke="#000" strokeWidth="2.5" />
            <ellipse cx="42" cy="50" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="50" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="50" r="3.5" fill="#000" />
            <circle cx="80" cy="50" r="3.5" fill="#000" />
          </svg>
        );
      case 'hoa':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 28, 105 28, 105 70 Z" fill="#a44651" stroke="#000" strokeWidth="2.5" />
            <path d="M 15 36 Q 60 52 105 36 L 105 70 L 15 70 Z" fill="#a44651" />
            <path d="M 15 36 Q 60 52 105 36" fill="none" stroke="#000" strokeWidth="2.5" />
            <ellipse cx="42" cy="52" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="52" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="52" r="3.5" fill="#000" />
            <circle cx="80" cy="52" r="3.5" fill="#000" />
          </svg>
        );
      case 'sinh':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 48, 22 36, 38 36 C 48 20, 72 20, 82 36 C 98 36, 105 48, 105 70 Z" fill="#523187" stroke="#000" strokeWidth="2.5" />
            <ellipse cx="42" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="48" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="48" r="3.5" fill="#000" />
            <circle cx="80" cy="48" r="3.5" fill="#000" />
          </svg>
        );
      case 'van':
        return (
          <svg viewBox="0 0 120 70" className="lp-neo-mascot-svg">
            <path d="M 15 70 C 15 28, 105 28, 105 70 Z" fill="#2d7783" stroke="#000" strokeWidth="2.5" />
            <polygon points="20,32 12,8 38,28" fill="#2d7783" stroke="#000" strokeWidth="2.5" strokeLinejoin="round" />
            <polygon points="100,32 108,8 82,28" fill="#2d7783" stroke="#000" strokeWidth="2.5" strokeLinejoin="round" />
            <ellipse cx="42" cy="50" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <ellipse cx="78" cy="50" rx="8" ry="6" fill="#fff" stroke="#000" strokeWidth="2" />
            <circle cx="44" cy="50" r="3.5" fill="#000" />
            <circle cx="80" cy="50" r="3.5" fill="#000" />
          </svg>
        );
      default:
        return null;
    }
  };

  useEffect(() => {
    const fn = () => setScrolled(window.scrollY > 10);
    window.addEventListener('scroll', fn);
    return () => window.removeEventListener('scroll', fn);
  }, []);

  const scrollToSection = (e, id) => {
    e.preventDefault();
    const el = document.getElementById(id);
    if (el) {
      el.scrollIntoView({ behavior: 'smooth', block: 'start' });
      setMobileMenuOpen(false);
    }
  };

  const maxIdx = FEEDBACK_LIST.length - itemsPerPage;
  const safeFeedbackIndex = Math.min(feedbackIndex, maxIdx >= 0 ? maxIdx : 0);

  return (
    <div className="lp-root">

      {/* ── NAVBAR ── */}
      {!['flashcards', 'ai-tutor', 'courses'].includes(activeLandingView) && !isExamTaking && (
        <nav className={`lp-nav${(scrolled || activeLandingView !== 'home') ? ' lp-nav--scrolled' : ''}`}>
          <div className="lp-nav__inner">
            <div className="lp-nav__logo" onClick={() => { if (navigateTo) { navigateTo('/'); } else { setActiveLandingView('home'); } }} style={{ display: 'flex', alignItems: 'center', gap: '10px', cursor: 'pointer' }}>
              <div className="lp-logo-box" style={{ background: 'none', boxShadow: 'none', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <img src={sunLogoImg} alt="EduPath AI" style={{ width: '38px', height: '38px', objectFit: 'contain' }} />
              </div>
              <span>EduPath <em>AI</em></span>
            </div>

            <div className="lp-nav__links">
              <a href="/courses" className={activeLandingView === 'courses' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } }}>Khóa học</a>
              <a href="/mock-exams" className={currentPath?.startsWith('/mock-exams') ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (!currentUser) { toast("Vui lòng đăng nhập để sử dụng chức năng thi thử!", 'warning'); if (onNavigateToAuth) onNavigateToAuth('login'); } else { if (navigateTo) { navigateTo('/mock-exams'); } else { setActiveLandingView('exams'); } } }}>Thi thử</a>
              <a href="/flashcards" className={currentPath === '/flashcards' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/flashcards'); } else { setActiveLandingView('features'); } }}>Flashcard</a>
              <a href="/ai-tutor" className={currentPath === '/ai-tutor' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/ai-tutor'); } else { setActiveLandingView('ai-tutor'); } }}>Mindmap</a>
              <a href="/exam-bank" className={currentPath === '/exam-bank' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/exam-bank'); } }}>Ngân hàng tài liệu</a>
              <a href="/leaderboard" className={currentPath === '/leaderboard' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (!currentUser) { toast("Vui lòng đăng nhập để xem bảng xếp hạng!", 'warning'); if (onNavigateToAuth) onNavigateToAuth('login'); } else { if (navigateTo) { navigateTo('/leaderboard'); } } }}>Bảng xếp hạng</a>
              <a href="/forum" className={activeLandingView === 'forum' ? 'lp-link--active' : ''} onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/forum'); } else { setActiveLandingView('forum'); } }}>Cộng đồng</a>
            </div>

            <div className="lp-nav__cta">
              {/* Desktop Cart Button */}
              <button
                onClick={() => {
                  if (cartCourses && cartCourses.length > 0) {
                    onCheckoutCourse(cartCourses[0]);
                  } else {
                    toast('Giỏ hàng trống! Hãy chọn một khóa học để thêm vào giỏ.', 'warning');
                  }
                }}
                style={{
                  background: 'rgba(255, 255, 255, 0.1)',
                  border: '1px solid rgba(255, 255, 255, 0.2)',
                  borderRadius: '50%',
                  width: '40px',
                  height: '40px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  cursor: 'pointer',
                  color: '#ffffff',
                  position: 'relative',
                  transition: 'all 0.2s ease',
                  marginRight: '8px',
                  outline: 'none'
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.background = 'rgba(255, 255, 255, 0.2)';
                  e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.3)';
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.background = 'rgba(255, 255, 255, 0.1)';
                  e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.2)';
                }}
                title="Giỏ hàng"
              >
                <HiShoppingCart style={{ fontSize: '20px' }} />
                {cartCourses && cartCourses.length > 0 && (
                  <span
                    style={{
                      position: 'absolute',
                      top: '-4px',
                      right: '-4px',
                      backgroundColor: '#ef4444',
                      color: '#ffffff',
                      fontSize: '10px',
                      fontWeight: 'bold',
                      borderRadius: '50%',
                      width: '18px',
                      height: '18px',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      border: '2px solid var(--lp-bg, #0f172a)',
                      boxShadow: '0 2px 4px rgba(0,0,0,0.2)'
                    }}
                  >
                    {cartCourses.length}
                  </span>
                )}
              </button>

              {currentUser ? (
                <div style={{ display: 'flex', alignItems: 'center', gap: '16px', position: 'relative' }}>
                  <button
                    className="lp-btn lp-btn--accent"
                    onClick={() => onBackToDashboard('home')}
                    style={{
                      background: 'linear-gradient(135deg, var(--lp-accent), #22c55e)',
                      boxShadow: '0 4px 14px rgba(74,222,128,0.3)',
                      border: 'none',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '6px'
                    }}
                  >
                    Vào học ngay 🚀
                  </button>

                  <button
                    onClick={() => {
                      setNotifDropdownOpen(!notifDropdownOpen);
                      setProfileDropdownOpen(false);
                    }}
                    style={{
                      background: 'rgba(255, 255, 255, 0.1)',
                      border: '1px solid rgba(255, 255, 255, 0.2)',
                      borderRadius: '50%',
                      width: '38px',
                      height: '38px',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      cursor: 'pointer',
                      color: '#ffffff',
                      position: 'relative',
                      transition: 'all 0.2s ease',
                      outline: 'none',
                      marginRight: '4px'
                    }}
                    onMouseEnter={(e) => {
                      e.currentTarget.style.background = 'rgba(255, 255, 255, 0.2)';
                      e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.3)';
                    }}
                    onMouseLeave={(e) => {
                      e.currentTarget.style.background = 'rgba(255, 255, 255, 0.1)';
                      e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.2)';
                    }}
                    title="Thông báo"
                  >
                    <HiBell style={{ fontSize: '18px' }} />
                    {unreadCount > 0 && (
                      <span
                        style={{
                          position: 'absolute',
                          top: '-3px',
                          right: '-3px',
                          backgroundColor: '#ef4444',
                          color: '#ffffff',
                          fontSize: '9px',
                          fontWeight: 'bold',
                          borderRadius: '50%',
                          width: '16px',
                          height: '16px',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          border: '1.5px solid var(--lp-bg, #0f172a)'
                        }}
                      >
                        {unreadCount}
                      </span>
                    )}
                  </button>

                  {/* Notifications Dropdown Menu */}
                  {notifDropdownOpen && (
                    <>
                      <div
                        onClick={() => setNotifDropdownOpen(false)}
                        style={{
                          position: 'fixed',
                          top: 0, left: 0, right: 0, bottom: 0,
                          zIndex: 998
                        }}
                      />
                      <div
                        className="lp-dropdown-menu animate-in"
                        style={{
                          position: 'absolute',
                          top: '46px',
                          right: '54px',
                          width: '320px',
                          background: 'var(--bg-card, #ffffff)',
                          borderRadius: '14px',
                          boxShadow: '0 10px 30px rgba(0,0,0,0.15)',
                          border: '1px solid var(--border, #e2e8f0)',
                          padding: '16px',
                          zIndex: 999,
                          textAlign: 'left'
                        }}
                      >
                        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px', borderBottom: '1px solid var(--border, #f1f5f9)', paddingBottom: '8px' }}>
                          <span style={{ fontSize: '13px', fontWeight: '850', color: 'var(--text-main, #1e293b)' }}>
                            🔔 THÔNG BÁO ({unreadCount})
                          </span>
                          {unreadCount > 0 && (
                            <button
                              onClick={() => {
                                onClearNotifications();
                                setNotifDropdownOpen(false);
                              }}
                              style={{ background: 'none', border: 'none', color: '#6c5ce7', fontSize: '11px', cursor: 'pointer', fontWeight: '700' }}
                            >
                              Đọc tất cả
                            </button>
                          )}
                        </div>

                        <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', maxHeight: '260px', overflowY: 'auto', paddingRight: '4px' }}>
                          {notifications.length > 0 ? (
                            notifications.slice(0, 5).map((n) => {
                              const icon = n.icon || (n.type === 'SUCCESS' ? '✅' : n.type === 'WARNING' ? '⚠️' : n.type === 'ERROR' ? '❌' : '🔔');
                              const borderLeftColor = n.type === 'SUCCESS' ? '#10B981' : n.type === 'WARNING' ? '#F59E0B' : n.type === 'ERROR' ? '#EF4444' : '#6c5ce7';
                              const isReadNotif = n.read || n.isRead;

                              return (
                                <div
                                  key={n.id}
                                  onClick={() => {
                                    if (navigateTo) {
                                      if (!isReadNotif) {
                                        api.markNotificationAsRead(n.id).catch(console.error);
                                      }
                                      if (n.link) navigateTo(n.link);
                                    }
                                    setNotifDropdownOpen(false);
                                  }}
                                  style={{
                                    padding: '8px 10px', borderRadius: '10px',
                                    background: isReadNotif ? 'transparent' : 'rgba(108, 92, 231, 0.04)',
                                    border: '1px solid var(--border, #e2e8f0)',
                                    borderLeft: `5px solid ${borderLeftColor}`,
                                    fontSize: '11.5px', lineHeight: '1.4',
                                    cursor: 'pointer',
                                    transition: 'all 0.15s ease',
                                    display: 'flex', gap: '8px', alignItems: 'flex-start'
                                  }}
                                >
                                  <div style={{ fontSize: '14px', marginTop: '1px' }}>{icon}</div>
                                  <div style={{ flex: 1, minWidth: 0 }}>
                                    <div style={{ fontWeight: '750', color: 'var(--text-main, #1e293b)', fontSize: '12px', marginBottom: '1px', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>{n.title || 'Thông báo'}</div>
                                    <p style={{ color: 'var(--text-muted, #64748b)', margin: 0, fontSize: '11px', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden' }}>{n.text || n.message}</p>
                                  </div>
                                </div>
                              );
                            })
                          ) : (
                            <p style={{ fontSize: '12px', color: 'var(--text-muted, #64748b)', textAlign: 'center', padding: '16px 0', fontWeight: '500' }}>Không có thông báo mới.</p>
                          )}
                        </div>

                        <div style={{ marginTop: '12px', borderTop: '1px solid var(--border, #f1f5f9)', paddingTop: '8px', textAlign: 'center' }}>
                          <button
                            onClick={() => {
                              setNotifDropdownOpen(false);
                              if (navigateTo) {
                                navigateTo('/user/notifications');
                              }
                            }}
                            style={{
                              background: 'none', border: 'none', color: '#6c5ce7',
                              fontSize: '11.5px', fontWeight: '700', cursor: 'pointer'
                            }}
                          >
                            Xem tất cả thông báo ➔
                          </button>
                        </div>
                      </div>
                    </>
                  )}

                  {/* Profile Avatar Trigger */}
                  <div style={{ position: 'relative' }}>
                    <button
                      onClick={() => setProfileDropdownOpen(!profileDropdownOpen)}
                      style={{
                        background: 'none',
                        border: 'none',
                        padding: 0,
                        cursor: 'pointer',
                        display: 'flex',
                        alignItems: 'center',
                        outline: 'none'
                      }}
                    >
                      <div
                        style={{
                          width: '38px',
                          height: '38px',
                          borderRadius: '50%',
                          background: 'linear-gradient(135deg, #6c5ce7, #a29bfe)',
                          color: '#fff',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          fontWeight: '800',
                          fontSize: '13px',
                          border: '2px solid rgba(255,255,255,0.2)',
                          boxShadow: '0 2px 10px rgba(0,0,0,0.15)',
                          overflow: 'hidden'
                        }}
                      >
                        {currentUser.avatar && (currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') || currentUser.avatar.length > 10) ? (
                          <img
                            src={currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') ? currentUser.avatar : `data:image/png;base64,${currentUser.avatar}`}
                            alt="Avatar"
                            style={{ width: '100%', height: '100%', objectFit: 'cover' }}
                          />
                        ) : (
                          (currentUser.avatar && currentUser.avatar.length <= 10) ? currentUser.avatar : (currentUser.fullName || currentUser.name || 'U').slice(0, 2).toUpperCase()
                        )}
                      </div>
                    </button>

                    {/* Profile Dropdown Menu */}
                    {profileDropdownOpen && (
                      <>
                        <div
                          onClick={() => setProfileDropdownOpen(false)}
                          style={{
                            position: 'fixed',
                            top: 0, left: 0, right: 0, bottom: 0,
                            zIndex: 998
                          }}
                        />
                        <div
                          className="lp-dropdown-menu animate-in"
                          style={{
                            position: 'absolute',
                            top: '46px',
                            right: 0,
                            width: '260px',
                            background: 'var(--bg-card, #ffffff)',
                            borderRadius: '14px',
                            boxShadow: '0 10px 30px rgba(0,0,0,0.15)',
                            border: '1px solid var(--border, #e2e8f0)',
                            padding: '16px',
                            zIndex: 999,
                            textAlign: 'left'
                          }}
                        >
                          <div style={{ display: 'flex', alignItems: 'center', gap: '12px', paddingBottom: '12px', borderBottom: '1px solid var(--border, #f1f5f9)' }}>
                            <div
                              style={{
                                width: '40px',
                                height: '40px',
                                borderRadius: '50%',
                                background: 'linear-gradient(135deg, #6c5ce7, #a29bfe)',
                                color: '#fff',
                                display: 'flex',
                                alignItems: 'center',
                                justifyContent: 'center',
                                fontWeight: 'bold',
                                fontSize: '14px',
                                flexShrink: 0,
                                overflow: 'hidden'
                              }}
                            >
                              {currentUser.avatar && (currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') || currentUser.avatar.length > 10) ? (
                                <img
                                  src={currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') ? currentUser.avatar : `data:image/png;base64,${currentUser.avatar}`}
                                  alt="Avatar"
                                  style={{ width: '100%', height: '100%', objectFit: 'cover' }}
                                />
                              ) : (
                                (currentUser.avatar && currentUser.avatar.length <= 10) ? currentUser.avatar : (currentUser.fullName || currentUser.name || 'U').slice(0, 2).toUpperCase()
                              )}
                            </div>
                            <div style={{ minWidth: 0 }}>
                              <h4 style={{ margin: 0, fontSize: '13.5px', fontWeight: '700', color: 'var(--text-main, #1e293b)', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
                                {currentUser.fullName || currentUser.name}
                              </h4>
                              <p style={{ margin: '2px 0 0', fontSize: '11px', color: 'var(--text-muted, #64748b)', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
                                {currentUser.email}
                              </p>
                              <span
                                style={{
                                  display: 'inline-block',
                                  background: 'rgba(108,92,231,0.12)',
                                  color: 'var(--primary, #6c5ce7)',
                                  fontSize: '8.5px',
                                  fontWeight: '800',
                                  padding: '1px 6px',
                                  borderRadius: '10px',
                                  marginTop: '4px',
                                  textTransform: 'uppercase'
                                }}
                              >
                                {currentUser.role === 'student' ? 'Học sinh' : (currentUser.role === 'teacher' ? 'Giáo viên' : 'Admin')}
                              </span>
                            </div>
                          </div>

                          <div style={{ display: 'flex', flexDirection: 'column', gap: '4px', marginTop: '10px' }}>
                            <button
                              onClick={() => { setProfileDropdownOpen(false); onBackToDashboard('home'); }}
                              style={{
                                display: 'flex',
                                alignItems: 'center',
                                gap: '10px',
                                width: '100%',
                                padding: '9px 10px',
                                background: 'none',
                                border: 'none',
                                borderRadius: '8px',
                                color: 'var(--text-secondary, #334155)',
                                fontSize: '13px',
                                fontWeight: '600',
                                cursor: 'pointer',
                                textAlign: 'left',
                                transition: 'background 0.2s'
                              }}
                              className="lp-dropdown-item"
                            >
                              💻 Bảng điều khiển
                            </button>

                            <button
                              onClick={() => { setProfileDropdownOpen(false); onBackToDashboard('settings'); }}
                              style={{
                                display: 'flex',
                                alignItems: 'center',
                                gap: '10px',
                                width: '100%',
                                padding: '9px 10px',
                                background: 'none',
                                border: 'none',
                                borderRadius: '8px',
                                color: 'var(--text-secondary, #334155)',
                                fontSize: '13px',
                                fontWeight: '600',
                                cursor: 'pointer',
                                textAlign: 'left',
                                transition: 'background 0.2s'
                              }}
                              className="lp-dropdown-item"
                            >
                              ⚙️ Thiết lập hồ sơ
                            </button>

                            <div style={{ height: '1px', background: 'var(--border, #f1f5f9)', margin: '4px 0' }} />

                            <button
                              onClick={() => { setProfileDropdownOpen(false); onLogout(); }}
                              style={{
                                display: 'flex',
                                alignItems: 'center',
                                gap: '10px',
                                width: '100%',
                                padding: '9px 10px',
                                background: 'none',
                                border: 'none',
                                borderRadius: '8px',
                                color: '#ef4444',
                                fontSize: '13px',
                                fontWeight: '600',
                                cursor: 'pointer',
                                textAlign: 'left',
                                transition: 'background 0.2s'
                              }}
                              className="lp-dropdown-item lp-dropdown-item--logout"
                            >
                              🚪 Đăng xuất
                            </button>
                          </div>
                        </div>
                      </>
                    )}
                  </div>
                </div>
              ) : (
                <button className="lp-btn lp-btn--accent lp-btn--start" onClick={() => onNavigateToAuth('signup')}>
                  Bắt đầu học
                </button>
              )}
            </div>

            <button
              onClick={() => {
                if (cartCourses && cartCourses.length > 0) {
                  onCheckoutCourse(cartCourses[0]);
                } else {
                  toast('Giỏ hàng trống! Hãy chọn một khóa học để thêm vào giỏ.', 'warning');
                }
              }}
              className="lp-mobile-cart-btn"
              style={{
                background: 'rgba(255, 255, 255, 0.1)',
                border: '1px solid rgba(255, 255, 255, 0.2)',
                borderRadius: '50%',
                width: '38px',
                height: '38px',
                alignItems: 'center',
                justifyContent: 'center',
                cursor: 'pointer',
                color: '#ffffff',
                position: 'relative',
                marginRight: '8px',
                outline: 'none'
              }}
              title="Giỏ hàng"
            >
              <HiShoppingCart style={{ fontSize: '18px' }} />
              {cartCourses && cartCourses.length > 0 && (
                <span
                  style={{
                    position: 'absolute',
                    top: '-3px',
                    right: '-3px',
                    backgroundColor: '#ef4444',
                    color: '#ffffff',
                    fontSize: '9px',
                    fontWeight: 'bold',
                    borderRadius: '50%',
                    width: '16px',
                    height: '16px',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    border: '1.5px solid var(--lp-bg, #0f172a)'
                  }}
                >
                  {cartCourses.length}
                </span>
              )}
            </button>

            <button className="lp-nav__hamburger" onClick={() => setMobileMenuOpen(!mobileMenuOpen)}>
              {mobileMenuOpen ? <HiX /> : <HiMenuAlt3 />}
            </button>
          </div>

          {mobileMenuOpen && (
            <div className="lp-mobile-menu">
              <a href="/courses" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } setMobileMenuOpen(false); }}>Khóa học</a>
              <a href="/mock-exams" onClick={(e) => { e.preventDefault(); if (!currentUser) { toast("Vui lòng đăng nhập để sử dụng chức năng thi thử!", 'warning'); if (onNavigateToAuth) onNavigateToAuth('login'); } else { if (navigateTo) { navigateTo('/mock-exams'); } else { setActiveLandingView('exams'); } } setMobileMenuOpen(false); }}>Thi thử</a>
              <a href="/flashcards" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/flashcards'); } else { setActiveLandingView('features'); } setMobileMenuOpen(false); }}>Flashcard</a>
              <a href="/ai-tutor" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/ai-tutor'); } else { setActiveLandingView('ai-tutor'); } setMobileMenuOpen(false); }}>Mindmap</a>
              <a href="/exam-bank" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/exam-bank'); } setMobileMenuOpen(false); }}>Ngân hàng tài liệu</a>
              <a href="/leaderboard" onClick={(e) => { e.preventDefault(); if (!currentUser) { toast("Vui lòng đăng nhập để xem bảng xếp hạng!", 'warning'); if (onNavigateToAuth) onNavigateToAuth('login'); } else { if (navigateTo) { navigateTo('/leaderboard'); } } setMobileMenuOpen(false); }}>Bảng xếp hạng</a>
              <a href="/forum" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/forum'); } else { setActiveLandingView('forum'); } setMobileMenuOpen(false); }}>Cộng đồng</a>

              {currentUser ? (
                <div style={{ display: 'flex', flexDirection: 'column', gap: '10px', marginTop: '12px', paddingTop: '12px', borderTop: '1px solid rgba(255,255,255,0.1)' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '4px' }}>
                    <div
                      style={{
                        width: '38px',
                        height: '38px',
                        borderRadius: '50%',
                        background: 'linear-gradient(135deg, #6c5ce7, #a29bfe)',
                        color: '#fff',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontWeight: 'bold',
                        fontSize: '13px',
                        overflow: 'hidden'
                      }}
                    >
                      {currentUser.avatar && (currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') || currentUser.avatar.length > 10) ? (
                        <img
                          src={currentUser.avatar.startsWith('data:') || currentUser.avatar.startsWith('http') ? currentUser.avatar : `data:image/png;base64,${currentUser.avatar}`}
                          alt="Avatar"
                          style={{ width: '100%', height: '100%', objectFit: 'cover' }}
                        />
                      ) : (
                        (currentUser.avatar && currentUser.avatar.length <= 10) ? currentUser.avatar : (currentUser.fullName || currentUser.name || 'U').slice(0, 2).toUpperCase()
                      )}
                    </div>
                    <div>
                      <h4 style={{ margin: 0, fontSize: '13.5px', fontWeight: '700', color: '#fff' }}>{currentUser.fullName || currentUser.name}</h4>
                      <p style={{ margin: '2px 0 0', fontSize: '11px', color: 'rgba(255,255,255,0.6)' }}>{currentUser.email}</p>
                    </div>
                  </div>

                  <button
                    className="lp-btn lp-btn--accent"
                    style={{ width: '100%', justifyContent: 'center' }}
                    onClick={() => { onBackToDashboard('home'); setMobileMenuOpen(false); }}
                  >
                    Vào học ngay 🚀
                  </button>
                  <button
                    className="lp-btn lp-btn--ghost"
                    style={{ width: '100%', justifyContent: 'center', borderColor: '#f87171', color: '#f87171' }}
                    onClick={() => { onLogout(); setMobileMenuOpen(false); }}
                  >
                    Đăng xuất
                  </button>
                </div>
              ) : (
                <div style={{ display: 'flex', gap: 10, marginTop: 8 }}>
                  <button className="lp-btn lp-btn--ghost" style={{ flex: 1 }} onClick={() => { onNavigateToAuth('login'); setMobileMenuOpen(false); }}>Đăng nhập</button>
                  <button className="lp-btn lp-btn--accent" style={{ flex: 1 }} onClick={() => { onNavigateToAuth('signup'); setMobileMenuOpen(false); }}>Đăng ký</button>
                </div>
              )}
            </div>
          )}
        </nav>
      )}

      {activeLandingView === 'courses' && (
        <Header
          role="guest"
          userProfile={currentUser}
          theme="light"
          onToggleTheme={() => { }}
          notifications={[]}
          onClearNotifications={() => { }}
          onLogout={onLogout}
          onChangePassword={() => { }}
          onNavigateSettings={null}
          addLog={() => { }}
          cartCourses={cartCourses}
          onCheckoutCourse={onCheckoutCourse}
          navigateTo={navigateTo}
          currentPath={currentPath}
          courses={courses}
        />
      )}

      <div style={{ paddingTop: (['flashcards', 'ai-tutor'].includes(activeLandingView) || isExamTaking) ? '0px' : (activeLandingView === 'courses' ? '0px' : '68px') }}>
        {activeLandingView === 'home' && (
          <div className="animate-fade">

            {/* ── HERO ── */}
            <section className="lp-hero">
              <div className="lp-hero__left">

                <div className="lp-hero__title-row">
                  <h2 className="lp-hero__title-main">Nền tảng</h2>
                  <span className="lp-hero__title-badge">EduPath AI</span>
                </div>

                <div className="lp-hero__highlight-box">
                  Luyện thi THPTQG
                  <span className="lp-hero__cursor">|</span>
                </div>

                <p className="lp-hero__sub">
                  Đầy đủ tài liệu, bài luyện, phương pháp và từ vựng giúp bạn chinh phục kỳ thi THPTQG dễ dàng hơn mỗi ngày.
                </p>


                <div className="lp-hero__actions">
                  <button className="lp-hero__btn-white" onClick={() => currentUser ? onBackToDashboard('forum') : onNavigateToAuth('signup')}>
                    Bài viết của EduPath
                    <HiArrowRight />
                  </button>
                  <button className="lp-hero__btn-explore" onClick={(e) => currentUser ? onBackToDashboard('library') : scrollToSection(e, 'subjects')}>
                    <svg stroke="currentColor" fill="none" strokeWidth="2.5" viewBox="0 0 24 24" strokeLinecap="round" strokeLinejoin="round" height="1.1em" width="1.1em" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10"></circle><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"></polygon></svg>
                    Khám phá tài liệu miễn phí
                  </button>
                </div>
              </div>

              <div className="lp-hero__right">
                <FooterSunMascot />
              </div>
            </section>

            {/* Flat horizontal divider */}
            <div className="lp-flat-divider" />

            {/* ── HIGHLY PREDICTIVE FEATURE CARDS (NEO-BRUTALIST STYLE) ── */}
            <section id="featured-exams" className="lp-subjects" style={{ background: '#FFFFFF', padding: '70px 0' }}>
              <div className="lp-container">


                <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: '28px', maxWidth: '1200px', margin: '0 auto' }}>

                  {/* Card 1: Flashcard */}
                  <div
                    onClick={() => {
                      if (navigateTo) {
                        navigateTo('/flashcards');
                      } else if (setActiveLandingView) {
                        setActiveLandingView('features');
                      }
                    }}
                    style={{
                      background: '#BFDBFE',
                      border: '3.5px solid #000',
                      borderRadius: '0px',
                      padding: '24px',
                      boxShadow: '6px 6px 0px #000',
                      display: 'flex',
                      flexDirection: 'column',
                      minHeight: '380px',
                      position: 'relative',
                      overflow: 'hidden',
                      cursor: 'pointer',
                      transition: 'all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275)'
                    }}
                    className="lp-feature-card-neo"
                    onMouseEnter={e => {
                      e.currentTarget.style.transform = 'translate(-4px, -4px)';
                      e.currentTarget.style.boxShadow = '10px 10px 0px #000';
                    }}
                    onMouseLeave={e => {
                      e.currentTarget.style.transform = 'none';
                      e.currentTarget.style.boxShadow = '6px 6px 0px #000';
                    }}
                  >
                    <div style={{ width: '42px', height: '42px', background: '#FFF', border: '2.5px solid #000', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '20px' }}>
                      <HiLightningBolt style={{ fontSize: '20px', color: '#000' }} />
                    </div>
                    <h4 style={{ fontSize: '22px', fontWeight: '950', color: '#000', margin: '0 0 10px', letterSpacing: '-0.5px' }}>Flashcard</h4>
                    <p style={{ fontSize: '13px', color: '#1E293B', margin: 0, lineHeight: 1.5, fontWeight: '600' }}>
                      Ghi nhớ nhanh các công thức toán học, vật lý, hóa học và từ vựng tiếng Anh qua hệ thống thẻ học thông minh và phương pháp Spaced Repetition.
                    </p>

                    {/* Mascot bottom decoration */}
                    <div style={{
                      position: 'absolute',
                      bottom: 0,
                      left: '50%',
                      transform: 'translateX(-50%)',
                      width: '130px',
                      height: '65px',
                      borderRadius: '65px 65px 0 0',
                      background: '#60A5FA',
                      display: 'flex',
                      flexDirection: 'column',
                      justifyContent: 'center',
                      alignItems: 'center',
                      paddingTop: '10px',
                      transition: 'height 0.2s'
                    }}>
                      {/* Eyes */}
                      <div style={{ display: 'flex', gap: '8px', marginBottom: '22px' }}>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000' }}></div>
                        </div>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000' }}></div>
                        </div>
                      </div>
                      {/* Pill badge */}
                      <div style={{
                        position: 'absolute',
                        bottom: '12px',
                        background: '#000',
                        color: '#FFF',
                        border: '2px solid #000',
                        padding: '4px 12px',
                        borderRadius: '20px',
                        fontSize: '10px',
                        fontWeight: '900',
                        boxShadow: '1.5px 1.5px 0px #FFE259',
                        whiteSpace: 'nowrap'
                      }}>
                        1000+ THẺ
                      </div>
                    </div>
                  </div>

                  {/* Card 2: Mindmap */}
                  <div
                    onClick={() => {
                      if (navigateTo) {
                        navigateTo('/ai-tutor');
                      } else if (setActiveLandingView) {
                        setActiveLandingView('ai-tutor');
                      }
                    }}
                    style={{
                      background: '#A7F3D0',
                      border: '3.5px solid #000',
                      borderRadius: '0px',
                      padding: '24px',
                      boxShadow: '6px 6px 0px #000',
                      display: 'flex',
                      flexDirection: 'column',
                      minHeight: '380px',
                      position: 'relative',
                      overflow: 'hidden',
                      cursor: 'pointer',
                      transition: 'all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275)'
                    }}
                    className="lp-feature-card-neo"
                    onMouseEnter={e => {
                      e.currentTarget.style.transform = 'translate(-4px, -4px)';
                      e.currentTarget.style.boxShadow = '10px 10px 0px #000';
                    }}
                    onMouseLeave={e => {
                      e.currentTarget.style.transform = 'none';
                      e.currentTarget.style.boxShadow = '6px 6px 0px #000';
                    }}
                  >
                    <div style={{ width: '42px', height: '42px', background: '#FFF', border: '2.5px solid #000', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '20px' }}>
                      <HiShare style={{ fontSize: '20px', color: '#000' }} />
                    </div>
                    <h4 style={{ fontSize: '22px', fontWeight: '950', color: '#000', margin: '0 0 10px', letterSpacing: '-0.5px' }}>Mindmap</h4>
                    <p style={{ fontSize: '13px', color: '#1E293B', margin: 0, lineHeight: 1.5, fontWeight: '600' }}>
                      Hệ thống sơ đồ tư duy thông minh tích hợp AI, giúp kết nối kiến thức bài học trực quan và hỗ trợ lượng giá năng lượng học tập tức thì.
                    </p>

                    {/* Mascot bottom decoration */}
                    <div style={{
                      position: 'absolute',
                      bottom: 0,
                      left: '50%',
                      transform: 'translateX(-50%)',
                      width: '130px',
                      height: '65px',
                      borderRadius: '65px 65px 0 0',
                      background: '#34D399',
                      display: 'flex',
                      flexDirection: 'column',
                      justifyContent: 'center',
                      alignItems: 'center',
                      paddingTop: '10px',
                      transition: 'height 0.2s'
                    }}>
                      {/* Eyes */}
                      <div style={{ display: 'flex', gap: '8px', marginBottom: '22px' }}>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-start', paddingLeft: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000' }}></div>
                        </div>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-start', paddingLeft: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000' }}></div>
                        </div>
                      </div>
                      {/* Pill badge */}
                      <div style={{
                        position: 'absolute',
                        bottom: '12px',
                        background: '#000',
                        color: '#FFF',
                        border: '2px solid #000',
                        padding: '4px 12px',
                        borderRadius: '20px',
                        fontSize: '10px',
                        fontWeight: '900',
                        boxShadow: '1.5px 1.5px 0px #FFE259',
                        whiteSpace: 'nowrap'
                      }}>
                        AI TỰ ĐỘNG
                      </div>
                    </div>
                  </div>

                  {/* Card 3: Thư viện tài liệu */}
                  <div
                    onClick={() => {
                      if (navigateTo) {
                        navigateTo('/exam-bank');
                      }
                    }}
                    style={{
                      background: '#FED7AA',
                      border: '3.5px solid #000',
                      borderRadius: '0px',
                      padding: '24px',
                      boxShadow: '6px 6px 0px #000',
                      display: 'flex',
                      flexDirection: 'column',
                      minHeight: '380px',
                      position: 'relative',
                      overflow: 'hidden',
                      cursor: 'pointer',
                      transition: 'all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275)'
                    }}
                    className="lp-feature-card-neo"
                    onMouseEnter={e => {
                      e.currentTarget.style.transform = 'translate(-4px, -4px)';
                      e.currentTarget.style.boxShadow = '10px 10px 0px #000';
                    }}
                    onMouseLeave={e => {
                      e.currentTarget.style.transform = 'none';
                      e.currentTarget.style.boxShadow = '6px 6px 0px #000';
                    }}
                  >
                    <div style={{ width: '42px', height: '42px', background: '#FFF', border: '2.5px solid #000', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '20px' }}>
                      <HiBookOpen style={{ fontSize: '20px', color: '#000' }} />
                    </div>
                    <h4 style={{ fontSize: '22px', fontWeight: '950', color: '#000', margin: '0 0 10px', letterSpacing: '-0.5px' }}>Thư viện tài liệu</h4>
                    <p style={{ fontSize: '13px', color: '#1E293B', margin: 0, lineHeight: 1.5, fontWeight: '600' }}>
                      Kho lưu trữ tài liệu, đề cương bài học và ngân hàng đề thi thử THPT Quốc Gia từ các trường chuyên danh tiếng trên cả nước.
                    </p>

                    {/* Mascot bottom decoration */}
                    <div style={{
                      position: 'absolute',
                      bottom: 0,
                      left: '50%',
                      transform: 'translateX(-50%)',
                      width: '130px',
                      height: '65px',
                      borderRadius: '65px 65px 0 0',
                      background: '#FB923C',
                      display: 'flex',
                      flexDirection: 'column',
                      justifyContent: 'center',
                      alignItems: 'center',
                      paddingTop: '10px',
                      transition: 'height 0.2s'
                    }}>
                      {/* Eyes */}
                      <div style={{ display: 'flex', gap: '8px', marginBottom: '22px' }}>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000', transform: 'translateY(-2px)' }}></div>
                        </div>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000', transform: 'translateY(-2px)' }}></div>
                        </div>
                      </div>
                      {/* Pill badge */}
                      <div style={{
                        position: 'absolute',
                        bottom: '12px',
                        background: '#000',
                        color: '#FFF',
                        border: '2px solid #000',
                        padding: '4px 12px',
                        borderRadius: '20px',
                        fontSize: '10px',
                        fontWeight: '900',
                        boxShadow: '1.5px 1.5px 0px #FFE259',
                        whiteSpace: 'nowrap'
                      }}>
                        FREE 100%
                      </div>
                    </div>
                  </div>

                  {/* Card 4: Cá nhân hóa */}
                  <div
                    onClick={() => {
                      if (currentUser) {
                        if (navigateTo) {
                          navigateTo('/user/home');
                        }
                      } else {
                        if (onNavigateToAuth) {
                          onNavigateToAuth('signup');
                        }
                        toast("Hãy đăng ký tài khoản để trải nghiệm Lộ trình học tập cá nhân hóa từ AI!", "info");
                      }
                    }}
                    style={{
                      background: '#DDD6FE',
                      border: '3.5px solid #000',
                      borderRadius: '0px',
                      padding: '24px',
                      boxShadow: '6px 6px 0px #000',
                      display: 'flex',
                      flexDirection: 'column',
                      minHeight: '380px',
                      position: 'relative',
                      overflow: 'hidden',
                      cursor: 'pointer',
                      transition: 'all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275)'
                    }}
                    className="lp-feature-card-neo"
                    onMouseEnter={e => {
                      e.currentTarget.style.transform = 'translate(-4px, -4px)';
                      e.currentTarget.style.boxShadow = '10px 10px 0px #000';
                    }}
                    onMouseLeave={e => {
                      e.currentTarget.style.transform = 'none';
                      e.currentTarget.style.boxShadow = '6px 6px 0px #000';
                    }}
                  >
                    <div style={{ width: '42px', height: '42px', background: '#FFF', border: '2.5px solid #000', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '20px' }}>
                      <HiSparkles style={{ fontSize: '20px', color: '#000' }} />
                    </div>
                    <h4 style={{ fontSize: '22px', fontWeight: '950', color: '#000', margin: '0 0 10px', letterSpacing: '-0.5px' }}>Cá nhân hóa</h4>
                    <p style={{ fontSize: '13px', color: '#1E293B', margin: 0, lineHeight: 1.5, fontWeight: '600' }}>
                      Phân tích tự động điểm mạnh và điểm yếu từ lịch sử làm bài để sinh ra Lộ trình học tập 7 ngày, kèm đề ôn tập thích ứng chuẩn xác nhất.
                    </p>

                    {/* Mascot bottom decoration */}
                    <div style={{
                      position: 'absolute',
                      bottom: 0,
                      left: '50%',
                      transform: 'translateX(-50%)',
                      width: '130px',
                      height: '65px',
                      borderRadius: '65px 65px 0 0',
                      background: '#A78BFA',
                      display: 'flex',
                      flexDirection: 'column',
                      justifyContent: 'center',
                      alignItems: 'center',
                      paddingTop: '10px',
                      transition: 'height 0.2s'
                    }}>
                      {/* Eyes */}
                      <div style={{ display: 'flex', gap: '8px', marginBottom: '22px' }}>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000', transform: 'rotate(15deg)' }}></div>
                        </div>
                        <div style={{ width: '22px', height: '16px', borderRadius: '50%', background: '#FFF', border: '2px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: '2px', position: 'relative' }}>
                          <div style={{ width: '6px', height: '6px', borderRadius: '50%', background: '#000', transform: 'rotate(15deg)' }}></div>
                        </div>
                      </div>
                      {/* Pill badge */}
                      <div style={{
                        position: 'absolute',
                        bottom: '12px',
                        background: '#000',
                        color: '#FFF',
                        border: '2px solid #000',
                        padding: '4px 12px',
                        borderRadius: '20px',
                        fontSize: '10px',
                        fontWeight: '900',
                        boxShadow: '1.5px 1.5px 0px #FFE259',
                        whiteSpace: 'nowrap'
                      }}>
                        LỘ TRÌNH AI
                      </div>
                    </div>
                  </div>

                </div>
              </div>
            </section>

            {/* ── GREEN STATISTICS & FPT/AI SYSTEM INTRO SECTION ── */}
            <section id="role-tabs-section" className="lp-subjects" style={{ background: '#FFF', padding: '70px 0' }}>
              <div className="lp-container" style={{ maxWidth: '1200px', margin: '0 auto', padding: '0 24px' }}>

                {/* Curvy Forest Green Card Container */}
                <div style={{
                  background: 'linear-gradient(135deg, #0e5b42 0%, #06402d 100%)',
                  borderRadius: '32px',
                  padding: '60px 48px 80px 48px',
                  position: 'relative',
                  overflow: 'hidden',
                  color: '#FFFFFF',
                  boxShadow: '0 12px 40px rgba(14, 91, 66, 0.15)',
                  textAlign: 'center'
                }}>
                  {/* Curved Connecting SVG Lines */}
                  <svg style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', pointerEvents: 'none', opacity: 0.18, zIndex: 1 }}>
                    <path d="M 120,120 C 300,40 500,240 700,120 C 850,40 920,160 980,240" fill="none" stroke="#FFFFFF" strokeWidth="3" strokeDasharray="6 6" />
                    <path d="M 180,280 C 260,160 480,280 620,140 C 720,280 880,280 940,160" fill="none" stroke="#FFFFFF" strokeWidth="3" />
                  </svg>

                  {/* Glowing Stars */}
                  <svg width="24" height="24" viewBox="0 0 24 24" style={{ position: 'absolute', left: '10%', top: '35%', fill: '#ffffff', opacity: 0.35, zIndex: 1 }}><path d="M12 0l3 9 9 3-9 3-3 9-3-9-9-3 9-3z" /></svg>
                  <svg width="18" height="18" viewBox="0 0 24 24" style={{ position: 'absolute', right: '18%', top: '25%', fill: '#ffffff', opacity: 0.45, zIndex: 1 }}><path d="M12 0l3 9 9 3-9 3-3 9-3-9-9-3 9-3z" /></svg>
                  <svg width="16" height="16" viewBox="0 0 24 24" style={{ position: 'absolute', left: '45%', top: '15%', fill: '#ffffff', opacity: 0.25, zIndex: 1 }}><path d="M12 0l3 9 9 3-9 3-3 9-3-9-9-3 9-3z" /></svg>

                  {/* Capsules Grid */}
                  <div style={{
                    display: 'grid',
                    gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))',
                    gap: '28px',
                    position: 'relative',
                    zIndex: 2,
                    maxWidth: '920px',
                    margin: '0 auto'
                  }}>
                    {/* Capsule 1: FPT University */}
                    <div style={{
                      background: '#FFFFFF',
                      borderRadius: '100px',
                      padding: '14px 28px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '16px',
                      boxShadow: '0 8px 24px rgba(0, 0, 0, 0.08)',
                      border: '1.5px solid #F1F5F9'
                    }}>
                      <div style={{
                        width: '44px',
                        height: '44px',
                        borderRadius: '50%',
                        backgroundColor: '#E0F2FE',
                        color: '#0284C7',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '20px',
                        flexShrink: 0
                      }}>
                        <HiCode />
                      </div>
                      <div style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
                        <span style={{ fontSize: '18px', fontWeight: '850', color: '#0F172A', lineHeight: '1.2' }}>Đại học FPT</span>
                        <span style={{ fontSize: '12px', fontWeight: '650', color: '#64748B', marginTop: '2px' }}>SV Kỹ thuật Phần mềm phát triển</span>
                      </div>
                    </div>

                    {/* Capsule 2: THPT Prep */}
                    <div style={{
                      background: '#FFFFFF',
                      borderRadius: '100px',
                      padding: '14px 28px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '16px',
                      boxShadow: '0 8px 24px rgba(0, 0, 0, 0.08)',
                      border: '1.5px solid #F1F5F9'
                    }}>
                      <div style={{
                        width: '44px',
                        height: '44px',
                        borderRadius: '50%',
                        backgroundColor: '#FEF3C7',
                        color: '#D97706',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '20px',
                        flexShrink: 0
                      }}>
                        <HiAcademicCap />
                      </div>
                      <div style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
                        <span style={{ fontSize: '18px', fontWeight: '850', color: '#0F172A', lineHeight: '1.2' }}>Cấp 3 & THPT</span>
                        <span style={{ fontSize: '12px', fontWeight: '650', color: '#64748B', marginTop: '2px' }}>Hỗ trợ ôn thi THPT Quốc Gia hiệu quả</span>
                      </div>
                    </div>

                    {/* Capsule 3: AI Application */}
                    <div style={{
                      background: '#FFFFFF',
                      borderRadius: '100px',
                      padding: '14px 28px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '16px',
                      boxShadow: '0 8px 24px rgba(0, 0, 0, 0.08)',
                      border: '1.5px solid #F1F5F9'
                    }}>
                      <div style={{
                        width: '44px',
                        height: '44px',
                        borderRadius: '50%',
                        backgroundColor: '#F3E8FF',
                        color: '#9333EA',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '20px',
                        flexShrink: 0
                      }}>
                        <HiSparkles />
                      </div>
                      <div style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
                        <span style={{ fontSize: '18px', fontWeight: '850', color: '#0F172A', lineHeight: '1.2' }}>Ứng dụng AI</span>
                        <span style={{ fontSize: '12px', fontWeight: '650', color: '#64748B', marginTop: '2px' }}>Cá nhân hóa lộ trình & sửa sai thông minh</span>
                      </div>
                    </div>

                    {/* Capsule 4: Outstanding Features */}
                    <div style={{
                      background: '#FFFFFF',
                      borderRadius: '100px',
                      padding: '14px 28px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '16px',
                      boxShadow: '0 8px 24px rgba(0, 0, 0, 0.08)',
                      border: '1.5px solid #F1F5F9'
                    }}>
                      <div style={{
                        width: '44px',
                        height: '44px',
                        borderRadius: '50%',
                        backgroundColor: '#D1FAE5',
                        color: '#059669',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '20px',
                        flexShrink: 0
                      }}>
                        <HiUserGroup />
                      </div>
                      <div style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
                        <span style={{ fontSize: '18px', fontWeight: '850', color: '#0F172A', lineHeight: '1.2' }}>Đa tính năng</span>
                        <span style={{ fontSize: '12px', fontWeight: '650', color: '#64748B', marginTop: '2px' }}>AI Tutor, Thi thử & Flashcard đồng hành</span>
                      </div>
                    </div>

                    {/* Capsule 5: Bottom Wide Target Statement */}
                    <div style={{
                      gridColumn: '1 / -1',
                      background: '#FFFFFF',
                      borderRadius: '100px',
                      padding: '16px 32px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: '16px',
                      boxShadow: '0 8px 24px rgba(0, 0, 0, 0.08)',
                      border: '1.5px solid #F1F5F9',
                      maxWidth: '780px',
                      margin: '12px auto 0 auto'
                    }}>
                      <div style={{
                        width: '44px',
                        height: '44px',
                        borderRadius: '50%',
                        backgroundColor: '#FFE259',
                        color: '#854D0E',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '20px',
                        flexShrink: 0
                      }}>
                        <HiStar />
                      </div>
                      <span style={{ fontSize: '13.5px', fontWeight: '650', color: '#334155', textAlign: 'left', lineHeight: '1.5' }}>
                        Hệ thống hỗ trợ học tập đắc lực giúp bạn <strong>chinh phục điểm số mong muốn</strong> trong kỳ thi THPT Quốc Gia nhờ sự kết hợp giữa công nghệ AI tiên tiến và kho học liệu tương tác chuẩn hóa.
                      </span>
                    </div>
                  </div>

                  {/* Mascot Peeking from bottom right */}
                  <div style={{
                    position: 'absolute',
                    right: '10px',
                    bottom: '-15px',
                    zIndex: 3,
                    pointerEvents: 'none'
                  }} className="lp-green-mascot-responsive">
                    <svg width="220" height="150" viewBox="0 0 220 150">
                      <path d="M 20,150 C 20,65 90,30 160,30 C 230,30 240,85 240,150 Z" fill="#ffffff" stroke="#053b27" strokeWidth="4" />
                      <path d="M 95,68 Q 104,62 112,68" fill="none" stroke="#053b27" strokeWidth="2.5" strokeLinecap="round" />
                      <path d="M 135,68 Q 144,62 152,68" fill="none" stroke="#053b27" strokeWidth="2.5" strokeLinecap="round" />
                      <rect x="100" y="76" width="8" height="18" rx="4" fill="#053b27" />
                      <rect x="140" y="76" width="8" height="18" rx="4" fill="#053b27" />
                      <circle cx="85" cy="100" r="10" fill="#fecdd3" opacity="0.8" />
                      <circle cx="160" cy="100" r="10" fill="#fecdd3" opacity="0.8" />
                      <path d="M 115,100 Q 123,107 131,100" fill="none" stroke="#053b27" strokeWidth="3" strokeLinecap="round" />
                      <path d="M 150,30 C 150,15 165,15 160,28" fill="none" stroke="#053b27" strokeWidth="3" strokeLinecap="round" />
                      <g transform="translate(35, 90)">
                        <path d="M 15,35 C 5,35 0,25 0,15 C 0,5 10,0 20,5 Q 25,7 28,15 C 32,25 25,35 15,35 Z" fill="#ffffff" stroke="#053b27" strokeWidth="3.5" />
                        <path d="M 12,8 Q 14,-5 22,-3 Q 26,0 24,10 Z" fill="#ffffff" stroke="#053b27" strokeWidth="3.5" />
                      </g>
                    </svg>
                  </div>
                </div>

                {/* Intro description Row below the green block */}
                <div style={{
                  display: 'flex',
                  flexWrap: 'wrap',
                  alignItems: 'center',
                  justifyContent: 'space-between',
                  gap: '32px',
                  marginTop: '48px',
                  textAlign: 'left'
                }}>
                  <div style={{ flex: '1 1 650px' }}>
                    <span style={{ fontSize: '13.5px', fontWeight: '800', color: '#64748B', textTransform: 'uppercase', letterSpacing: '0.8px' }}>
                      Sản phẩm nghiên cứu học đường
                    </span>
                    <h2 style={{
                      fontSize: '38px',
                      fontWeight: '950',
                      color: '#0e5b42',
                      margin: '8px 0 16px 0',
                      letterSpacing: '-1px',
                      lineHeight: 1.15
                    }}>
                      HỆ THỐNG EDUPATH AI
                    </h2>
                    <p style={{
                      fontSize: '15.5px',
                      color: '#475569',
                      lineHeight: '1.65',
                      fontWeight: '500',
                      margin: 0
                    }}>
                      Được xây dựng bởi các nhóm sinh viên tại đại học FPT chuyên ngành kĩ thuật phần mềm nhằm hỗ trợ cho các bạn học sinh cấp 3 ôn thi THPT hiệu quả hơn nhờ sự ứng dụng AI và các tính năng học tập nổi bật.
                    </p>
                  </div>

                  <div style={{ flex: '0 0 auto', display: 'flex', alignItems: 'center' }}>
                    <button
                      onClick={(e) => {
                        const el = document.getElementById('featured-exams');
                        if (el) {
                          el.scrollIntoView({ behavior: 'smooth' });
                        } else {
                          if (navigateTo) navigateTo('/courses');
                        }
                      }}
                      style={{
                        background: '#FFFFFF',
                        color: '#0e5b42',
                        fontSize: '14.5px',
                        fontWeight: '800',
                        padding: '14px 28px',
                        border: '2px solid #0e5b42',
                        borderRadius: '100px',
                        cursor: 'pointer',
                        display: 'flex',
                        alignItems: 'center',
                        gap: '12px',
                        transition: 'all 0.2s ease',
                        boxShadow: '0 4px 12px rgba(14, 91, 66, 0.08)'
                      }}
                      onMouseEnter={(e) => {
                        e.currentTarget.style.background = '#0e5b42';
                        e.currentTarget.style.color = '#FFFFFF';
                        e.currentTarget.style.transform = 'translateY(-1px)';
                        e.currentTarget.style.boxShadow = '0 6px 16px rgba(14, 91, 66, 0.15)';
                      }}
                      onMouseLeave={(e) => {
                        e.currentTarget.style.background = '#FFFFFF';
                        e.currentTarget.style.color = '#0e5b42';
                        e.currentTarget.style.transform = 'none';
                        e.currentTarget.style.boxShadow = '0 4px 12px rgba(14, 91, 66, 0.08)';
                      }}
                    >
                      <span>Trải nghiệm học tập ngay</span>
                      <div style={{
                        width: '26px',
                        height: '26px',
                        borderRadius: '50%',
                        backgroundColor: '#0e5b42',
                        color: '#FFFFFF',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        fontSize: '12px',
                        transition: 'all 0.2s ease'
                      }}
                        className="lp-green-btn-arrow"
                      >
                        <HiArrowRight />
                      </div>
                    </button>
                  </div>
                </div>
              </div>
            </section>





            {/* ── CURRICULUM PREVIEW MODAL ── */}
            {selectedPreviewCourse && (
              <div className="checkout-overlay" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 10000, background: 'rgba(28, 40, 15, 0.45)', backdropFilter: 'blur(6px)' }}>
                <div className="checkout-modal lp-modal-neo animate-in">
                  <button
                    onClick={() => setSelectedPreviewCourse(null)}
                    style={{ position: 'absolute', top: '16px', right: '16px', border: 'none', background: 'none', color: '#111111', fontSize: '20px', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', width: '32px', height: '32px', borderRadius: '50%', border: '2px solid #000', background: '#fff', boxShadow: '2px 2px 0px #000' }}
                  >
                    <HiX />
                  </button>
                  <div style={{ display: 'flex', gap: '8px', alignItems: 'center', marginBottom: '10px' }}>
                    <span className="lp-course-subject-badge" style={{ background: '#FFC229' }}>
                      Đề cương bài học
                    </span>
                  </div>
                  <h3 style={{ fontSize: '18px', fontWeight: '900', marginBottom: '4px', color: '#111111', letterSpacing: '-0.5px' }}>
                    {selectedPreviewCourse.title}
                  </h3>
                  <p style={{ fontSize: '12.5px', color: '#4B5563', marginBottom: '20px', fontWeight: '700' }}>
                    Giảng dạy bởi: <span style={{ color: '#1C280F' }}>{selectedPreviewCourse.teacherName}</span>
                  </p>

                  <div style={{ display: 'flex', flexDirection: 'column', gap: '10px', maxHeight: '280px', overflowY: 'auto', paddingRight: '4px', marginBottom: '24px' }}>
                    {selectedPreviewCourse.lessons.map((lesson, idx) => (
                      <div key={idx} className="lp-modal-lesson-row">
                        <div style={{ display: 'flex', gap: '12px', alignItems: 'center' }}>
                          <span style={{ fontSize: '13px', color: '#5b75f3', fontWeight: '900', background: '#ffffff', width: '24px', height: '24px', borderRadius: '50%', border: '1.5px solid #000', display: 'flex', alignItems: 'center', justifyContent: 'center', boxShadow: '1px 1px 0px #000' }}>{idx + 1}</span>
                          <div>
                            <h5 style={{ fontSize: '12.5px', fontWeight: '800', color: '#111111', margin: 0 }}>{lesson.name}</h5>
                            <span style={{ fontSize: '10.5px', color: '#6B7280', fontWeight: '600' }}>{lesson.duration}</span>
                          </div>
                        </div>

                        {lesson.isFree ? (
                          <button
                            className="badge-pill"
                            style={{ background: '#22C55E', color: '#ffffff', border: '2px solid #000000', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '4px', padding: '6px 12px', fontSize: '10.5px', fontWeight: '900', boxShadow: '1.5px 1.5px 0px #000' }}
                            onClick={() => {
                              setSelectedLessonPlayer({ course: selectedPreviewCourse, lesson });
                              setSelectedPreviewCourse(null);
                              setActivePlayerTab('notes');
                              setLessonQuizAnswers({});
                              setLessonQuizSubmitted(false);
                              setLessonVideoPlaying(false);
                              setLessonChatHistory([
                                { role: 'assistant', content: `Chào bạn! Mình là AI Tutor. Bạn cần giải thích phần nào trong bài học "${lesson.name}" thuộc chuyên đề "${selectedPreviewCourse.title}"?` }
                              ]);
                            }}
                          >
                            <HiPlay /> Học thử
                          </button>
                        ) : (
                          <span style={{ fontSize: '11px', color: '#6B7280', display: 'flex', alignItems: 'center', gap: '4px', fontWeight: '800' }}>
                            <HiLockClosed /> Premium
                          </span>
                        )}
                      </div>
                    ))}
                  </div>

                  <button
                    className="lp-btn lp-btn--accent lp-btn--full"
                    onClick={() => {
                      setSelectedPreviewCourse(null);
                      if (currentUser) {
                        onBackToDashboard('courses');
                      } else {
                        onNavigateToAuth('signup');
                      }
                    }}
                  >
                    Mở khóa toàn bộ khóa học ngay
                  </button>
                </div>
              </div>
            )}





            {/* ── REGISTRATION FORM ── */}
            <section id="pricing" className="lp-pricing-section" style={{ backgroundColor: '#FFFFFF', padding: '80px 0' }}>
              <div className="lp-container" style={{ maxWidth: '1000px', margin: '0 auto', padding: '0 20px' }}>
                <div style={{
                  display: 'flex',
                  flexWrap: 'wrap',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: '48px'
                }}>
                  {/* Left side: Mascot */}
                  <div style={{ flex: '1 1 380px', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
                    <img
                      src={sunMascotImg}
                      alt="Sun Mascot"
                      style={{
                        width: '100%',
                        maxWidth: '440px',
                        height: 'auto',
                        objectFit: 'contain',
                        transform: 'scale(1.05)',
                        transition: 'transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275)'
                      }}
                      onMouseEnter={e => e.currentTarget.style.transform = 'scale(1.12) rotate(2deg)'}
                      onMouseLeave={e => e.currentTarget.style.transform = 'scale(1.05)'}
                    />
                  </div>

                  {/* Right side: Form */}
                  <div style={{ flex: '1 1 450px', display: 'flex', flexDirection: 'column', gap: '20px' }}>
                    <div style={{ textAlign: 'left' }}>
                      <h2 style={{
                        fontSize: '38px',
                        fontWeight: '900',
                        color: '#1E293B',
                        marginBottom: '10px',
                        lineHeight: '1.2',
                        letterSpacing: '-0.5px',
                        fontFamily: 'system-ui, -apple-system, sans-serif'
                      }}>
                        Nhận tài liệu <br />
                        luyện thi <span style={{ color: '#D97706' }}>miễn phí</span>
                      </h2>
                      <p style={{ fontSize: '15px', color: '#475569', lineHeight: '1.6', margin: 0, fontFamily: 'system-ui, -apple-system, sans-serif' }}>
                        Nhập thông tin để nhận ngay bộ đề mới nhất kèm tài liệu ôn thi chất lượng cao.
                      </p>
                    </div>

                    <form onSubmit={handleLeadSubmit} style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
                      {/* Input 1: Name */}
                      <div style={{ position: 'relative' }}>
                        <HiUser style={{
                          position: 'absolute',
                          left: '16px',
                          top: '50%',
                          transform: 'translateY(-50%)',
                          color: '#94A3B8',
                          fontSize: '18px'
                        }} />
                        <input
                          type="text"
                          placeholder="Họ và tên"
                          value={leadName}
                          onChange={(e) => setLeadName(e.target.value)}
                          style={{
                            width: '100%',
                            padding: '14px 16px 14px 46px',
                            border: '2px solid #E2E8F0',
                            borderRadius: '12px',
                            outline: 'none',
                            fontSize: '15px',
                            backgroundColor: '#FFFFFF',
                            color: '#1E293B',
                            boxShadow: 'inset 0 2px 4px rgba(0, 0, 0, 0.02)',
                            transition: 'border-color 0.2s',
                            fontFamily: 'system-ui, -apple-system, sans-serif'
                          }}
                          onFocus={(e) => e.target.style.borderColor = '#D97706'}
                          onBlur={(e) => e.target.style.borderColor = '#E2E8F0'}
                        />
                      </div>

                      {/* Input 2: Email */}
                      <div style={{ position: 'relative' }}>
                        <HiMail style={{
                          position: 'absolute',
                          left: '16px',
                          top: '50%',
                          transform: 'translateY(-50%)',
                          color: '#94A3B8',
                          fontSize: '18px'
                        }} />
                        <input
                          type="email"
                          placeholder="Email"
                          value={leadEmail}
                          onChange={(e) => setLeadEmail(e.target.value)}
                          style={{
                            width: '100%',
                            padding: '14px 16px 14px 46px',
                            border: '2px solid #E2E8F0',
                            borderRadius: '12px',
                            outline: 'none',
                            fontSize: '15px',
                            backgroundColor: '#FFFFFF',
                            color: '#1E293B',
                            boxShadow: 'inset 0 2px 4px rgba(0, 0, 0, 0.02)',
                            transition: 'border-color 0.2s',
                            fontFamily: 'system-ui, -apple-system, sans-serif'
                          }}
                          onFocus={(e) => e.target.style.borderColor = '#D97706'}
                          onBlur={(e) => e.target.style.borderColor = '#E2E8F0'}
                        />
                      </div>

                      {/* Input 3: Phone */}
                      <div style={{ position: 'relative' }}>
                        <HiPhone style={{
                          position: 'absolute',
                          left: '16px',
                          top: '50%',
                          transform: 'translateY(-50%)',
                          color: '#94A3B8',
                          fontSize: '18px'
                        }} />
                        <input
                          type="tel"
                          placeholder="Số điện thoại"
                          value={leadPhone}
                          onChange={(e) => setLeadPhone(e.target.value)}
                          style={{
                            width: '100%',
                            padding: '14px 16px 14px 46px',
                            border: '2px solid #E2E8F0',
                            borderRadius: '12px',
                            outline: 'none',
                            fontSize: '15px',
                            backgroundColor: '#FFFFFF',
                            color: '#1E293B',
                            boxShadow: 'inset 0 2px 4px rgba(0, 0, 0, 0.02)',
                            transition: 'border-color 0.2s',
                            fontFamily: 'system-ui, -apple-system, sans-serif'
                          }}
                          onFocus={(e) => e.target.style.borderColor = '#D97706'}
                          onBlur={(e) => e.target.style.borderColor = '#E2E8F0'}
                        />
                      </div>

                      {/* Submit Button */}
                      <button
                        type="submit"
                        disabled={leadSubmitting}
                        style={{
                          width: '100%',
                          padding: '14px 20px',
                          backgroundColor: '#E05A2B',
                          color: '#FFFFFF',
                          border: '2.5px solid #000000',
                          borderRadius: '12px',
                          fontSize: '16px',
                          fontWeight: '800',
                          cursor: 'pointer',
                          boxShadow: '3px 3px 0px #000000',
                          transition: 'all 0.2s ease',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          gap: '8px',
                          fontFamily: 'system-ui, -apple-system, sans-serif'
                        }}
                        onMouseEnter={(e) => {
                          e.currentTarget.style.transform = 'translate(-2px, -2px)';
                          e.currentTarget.style.boxShadow = '5px 5px 0px #000000';
                        }}
                        onMouseLeave={(e) => {
                          e.currentTarget.style.transform = 'none';
                          e.currentTarget.style.boxShadow = '3px 3px 0px #000000';
                        }}
                      >
                        {leadSubmitting ? 'Đang gửi...' : 'Gửi! →'}
                      </button>

                      {/* Secure Badge */}
                      <div style={{
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        gap: '6px',
                        fontSize: '11px',
                        color: '#94A3B8',
                        fontWeight: '700',
                        letterSpacing: '0.5px',
                        textTransform: 'uppercase',
                        marginTop: '4px',
                        fontFamily: 'system-ui, -apple-system, sans-serif'
                      }}>
                        <HiLockClosed style={{ fontSize: '13px' }} />
                        <span>Thông tin của bạn được bảo mật tuyệt đối</span>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </section>




          </div>
        )}

        {/* ================= 3. AI FEATURES DEDICATED PAGE ================= */}
        {activeLandingView === 'features' && (
          <div className="lp-container animate-fade" style={{ padding: '20px 28px 80px', backgroundColor: '#F5F1E8' }}>
            <button
              onClick={() => { if (navigateTo) { navigateTo('/'); } else { setActiveLandingView('home'); } }}
              style={{
                background: 'none',
                border: 'none',
                color: '#059669',
                fontWeight: 'bold',
                cursor: 'pointer',
                display: 'flex',
                alignItems: 'center',
                gap: '8px',
                fontSize: '14.5px',
                marginBottom: '28px',
                padding: 0
              }}
            >
              ← Quay lại trang chủ
            </button>
            <LearningPath currentUser={currentUser} navigateTo={navigateTo} />
          </div>
        )}

        {/* ================= 2. SUBJECTS DEDICATED PAGE ================= */}
        {activeLandingView === 'courses' && (
          <div className="animate-in" style={{ background: '#F5F1E8', minHeight: '100vh', padding: '16px 0 80px' }}>
            {!selectedCourseId && (
              <div style={{ marginBottom: '12px', maxWidth: '1440px', margin: '0 auto', padding: '0 24px' }}>
                <button
                  onClick={() => {
                    if (navigateTo) { navigateTo('/'); } else { setActiveLandingView('home'); }
                  }}
                  style={{
                    background: 'none',
                    border: 'none',
                    color: 'var(--emerald-primary, #059669)',
                    fontWeight: 'bold',
                    cursor: 'pointer',
                    display: 'flex',
                    alignItems: 'center',
                    gap: '8px',
                    fontSize: '14.5px',
                    padding: 0
                  }}
                >
                  ← Quay lại trang chủ
                </button>
              </div>
            )}

            {selectedCourseId ? (
              <CourseDetailPage
                courseId={selectedCourseId}
                currentUser={currentUser}
                onNavigateToLearn={onNavigateToLearn}
                onUpdateUser={onUpdateUser}
                onAddToCart={onAddToCart}
                onCheckoutCourse={onCheckoutCourse}
                navigateTo={navigateTo}
              />
            ) : (
              <CoursesPage
                courses={courses}
                currentUser={currentUser}
                onSelectCourse={(course) => {
                  setSelectedCourseId(course.id);
                  if (navigateTo) navigateTo(`/courses/${course.id}`);
                }}
                onCheckoutCourse={onCheckoutCourse}
                navigateTo={navigateTo}
              />
            )}
          </div>
        )}




        {/* ================= 4. ABOUT US DEDICATED PAGE ================= */}
        {activeLandingView === 'about' && (
          <div className="lp-container" style={{ padding: '40px 28px 80px' }}>
            <button
              onClick={() => { if (navigateTo) { navigateTo('/'); } else { setActiveLandingView('home'); } }}
              style={{
                background: 'none',
                border: 'none',
                color: 'var(--primary, #6c5ce7)',
                fontWeight: 'bold',
                cursor: 'pointer',
                display: 'flex',
                alignItems: 'center',
                gap: '8px',
                fontSize: '14.5px',
                marginBottom: '28px',
                padding: 0
              }}
            >
              ← Quay lại trang chủ
            </button>

            <div className="lp-modal-grid">
              <div className="lp-modal-img-col" style={{ backgroundImage: `url(${educatorsTeamImg})`, minHeight: '450px', borderRadius: '16px', overflow: 'hidden' }}>
                <div className="lp-modal-img-overlay">
                  <span className="lp-modal-badge" style={{ background: '#10b981' }}>🎓 Sứ Mệnh Chuyển Đổi Số</span>
                  <h3>Công Nghệ Tiên Phong · Giáo Dục Tận Tâm</h3>
                </div>
              </div>
              <div className="lp-modal-info-col" style={{ padding: '0 10px' }}>
                <span className="lp-eyebrow">VỀ CHÚNG TÔI</span>
                <h2 style={{ fontSize: '32px', fontWeight: '900', color: 'var(--lp-dark)', margin: '10px 0 18px 0' }}>Đại diện tiêu biểu đổi mới sáng tạo giáo dục</h2>
                <p style={{ fontSize: '14.5px', lineHeight: '1.7', color: 'var(--text-secondary)' }}>EduPath AI tự hào mang lại một giải pháp luyện thi THPTQG thích ứng số một Việt Nam, ứng dụng tinh hoa công nghệ trí tuệ nhân tạo và giáo trình giảng dạy chuyên sâu.</p>

                <div style={{ display: 'flex', flexDirection: 'column', gap: '14px', marginTop: '24px', fontSize: '13.5px', color: 'var(--text-secondary)' }}>
                  <div style={{ borderLeft: '4px solid var(--primary)', paddingLeft: '16px', background: 'var(--bg-card)', padding: '14px 16px', borderRadius: '0 12px 12px 0', border: '1px solid var(--border)', borderLeftWidth: '4px' }}>
                    <strong style={{ color: 'var(--text-main)', fontSize: '14.5px' }}>Sứ mệnh giáo dục:</strong> Phổ cập giáo dục chất lượng cao cá nhân hóa đến mọi học sinh Việt Nam, bất kể vùng miền với mức chi phí tối giản.
                  </div>
                  <div style={{ borderLeft: '4px solid var(--accent-orange)', paddingLeft: '16px', background: 'var(--bg-card)', padding: '14px 16px', borderRadius: '0 12px 12px 0', border: '1px solid var(--border)', borderLeftWidth: '4px' }}>
                    <strong style={{ color: 'var(--text-main)', fontSize: '14.5px' }}>Giá trị cốt lõi:</strong> Sáng tạo đột phá, Học sinh là trung tâm, Lấy uy tín giảng dạy và chất lượng đầu ra làm kim chỉ nam.
                  </div>
                  <div style={{ borderLeft: '4px solid var(--accent-green)', paddingLeft: '16px', background: 'var(--bg-card)', padding: '14px 16px', borderRadius: '0 12px 12px 0', border: '1px solid var(--border)', borderLeftWidth: '4px' }}>
                    <strong style={{ color: 'var(--text-main)', fontSize: '14.5px' }}>Đối tác sư phạm:</strong> Liên kết chặt chẽ với tổ chức khảo thí quốc tế và đội ngũ giảng viên chuyên sâu từ các trường chuyên danh tiếng (Amsterdam, Chuyên Sư Phạm).
                  </div>
                </div>

                <button
                  className="lp-btn lp-btn--accent lp-btn--full lp-btn--lg"
                  style={{ marginTop: '36px' }}
                  onClick={() => { if (currentUser) { onBackToDashboard('forum'); } else { onNavigateToAuth('signup'); } }}
                >
                  {currentUser ? "Tham gia diễn đàn thảo luận ngay 🚀" : "Khám phá cộng đồng học tập EduPath"}
                </button>
              </div>
            </div>
          </div>
        )}



        {/* ================= 3. THPTQG MOCK EXAMS DEDICATED PAGE ================= */}
        {activeLandingView === 'exams' && (
          <div className="lp-container" style={{ padding: '40px 28px 80px', minHeight: '80vh' }}>
            {(parsedRoute.route === 'public-mock-exams' || parsedRoute.route === 'mock-exams-list') && (
              <MockExamsPage
                currentUser={currentUser}
                onSelectExam={(examId) => navigateTo(`/mock-exams/${examId}`)}
                navigateTo={navigateTo}
              />
            )}

            {(parsedRoute.route === 'public-mock-exam-detail' || parsedRoute.route === 'mock-exam-detail') && (
              <MockExamDetailPage
                examId={parsedRoute.examId}
                currentUser={currentUser}
                onStartExam={(examId) => navigateTo(`/mock-exams/${examId}/start`)}
                navigateTo={navigateTo}
              />
            )}

            {(parsedRoute.route === 'public-mock-exam-taking' || parsedRoute.route === 'mock-exam-taking') && (
              <MockExamTakingPage
                examId={parsedRoute.examId}
                currentUser={currentUser}
                onFinished={(examId, attemptId) => navigateTo(`/mock-exams/${examId}/result/${attemptId}`)}
                navigateTo={navigateTo}
              />
            )}

            {(parsedRoute.route === 'public-mock-exam-result' || parsedRoute.route === 'mock-exam-result') && (
              <MockExamResultPage
                examId={parsedRoute.examId}
                attemptId={parsedRoute.attemptId}
                currentUser={currentUser}
                navigateTo={navigateTo}
              />
            )}
          </div>
        )}

        {/* ================= 6. FORUM DEDICATED PAGE ================= */}
        {activeLandingView === 'forum' && (
          <div style={{ maxWidth: '95%', margin: '0 auto', padding: '40px 28px 80px' }}>
            <button
              onClick={() => { if (navigateTo) { navigateTo('/'); } else { setActiveLandingView('home'); } }}
              style={{
                background: 'none',
                border: 'none',
                color: 'var(--primary, #6c5ce7)',
                fontWeight: 'bold',
                cursor: 'pointer',
                display: 'flex',
                alignItems: 'center',
                gap: '8px',
                fontSize: '14.5px',
                marginBottom: '28px',
                padding: 0
              }}
            >
              ← Quay lại trang chủ
            </button>

            {!currentUser && (
              <div style={{
                background: 'linear-gradient(135deg, rgba(108,92,231,0.1), rgba(91,117,243,0.1))',
                border: '1px solid var(--primary)',
                borderRadius: '16px',
                padding: '16px 20px',
                marginBottom: '24px',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                flexWrap: 'wrap',
                gap: '12px'
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
                  <span style={{ fontSize: '24px' }}>📢</span>
                  <div style={{ textAlign: 'left' }}>
                    <h4 style={{ margin: 0, fontSize: '14.5px', fontWeight: '800', color: 'var(--text-main)' }}>Bạn đang truy cập Diễn đàn với tư cách khách</h4>
                    <p style={{ margin: '2px 0 0 0', fontSize: '12px', color: 'var(--text-secondary)' }}>Đăng ký tài khoản miễn phí để thảo luận bài tập, đăng câu hỏi và nhận giải đáp từ giáo viên!</p>
                  </div>
                </div>
                <button
                  className="lp-btn lp-btn--accent"
                  onClick={() => onNavigateToAuth('signup')}
                  style={{ padding: '8px 16px', fontSize: '12.5px', fontWeight: 'bold' }}
                >
                  Đăng ký miễn phí
                </button>
              </div>
            )}

            <Forum
              forumPosts={forumPosts}
              onAddPost={handleAddPostPublic}
              onLikePost={handleLikePostPublic}
              onAddComment={handleAddCommentPublic}
              onAcceptCommentSolution={onAcceptCommentSolution}
              currentUser={currentUser}
            />
          </div>
        )}

        {/* ================= AI TUTOR DEDICATED PAGE ================= */}
        {activeLandingView === 'ai-tutor' && (
          <div style={{ width: '100%', maxWidth: '100%', margin: 0, padding: 0 }}>
            <AITutorPage currentUser={currentUser} navigateTo={navigateTo} hideHeader={false} />
          </div>
        )}

        {/* ================= FLASHCARDS DEDICATED PAGE ================= */}
        {activeLandingView === 'flashcards' && (
          <div style={{ width: '100%', maxWidth: '100%', margin: 0, padding: 0 }}>
            <FlashcardPage currentUser={currentUser} navigateTo={navigateTo} />
          </div>
        )}

        {/* ================= EXAM BANK DEDICATED PAGE ================= */}
        {activeLandingView === 'exam-bank' && (
          <div style={{ width: '100%', maxWidth: '100%', margin: 0, padding: 0 }}>
            <ExamBankPage currentUser={currentUser} navigateTo={navigateTo} hideHeader={true} />
          </div>
        )}

        {/* ================= LEADERBOARD DEDICATED PAGE ================= */}
        {activeLandingView === 'leaderboard' && (
          <div style={{ width: '100%', maxWidth: '1200px', margin: '0 auto', padding: '40px 20px' }}>
            <div className="card animate-in" style={{ border: '1px solid #E2E8F0', boxShadow: '0 10px 30px rgba(0, 0, 0, 0.04)', padding: '28px', background: '#FFFFFF', borderRadius: '16px' }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '1px solid #E2E8F0', paddingBottom: '16px', marginBottom: '20px' }}>
                <div>
                  <h3 style={{ fontSize: '20px', fontWeight: '950', color: '#1E293B', margin: 0 }}>
                    🏆 BẢNG XẾP HẠNG HỌC VIÊN
                  </h3>
                  <p style={{ fontSize: '13px', color: '#64748B', margin: '4px 0 0 0' }}>
                    Bảng vàng vinh danh những chiến thần học tập chăm chỉ và đạt thành tích cao nhất.
                  </p>
                </div>
                <span style={{ fontSize: '32px' }}>🏆</span>
              </div>
              <LeaderboardTab currentUser={currentUser} />
            </div>
          </div>
        )}

      </div>

      {/* ── LESSON PLAYER MODAL OVERLAY (GUEST TRYOUT) ── */}
      {selectedLessonPlayer && (
        <div className="checkout-overlay" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 10000, background: 'rgba(15,23,42,0.85)', backdropFilter: 'blur(10px)' }}>
          <div className="lp-lesson-player-modal animate-in">
            {/* Header */}
            <div className="lp-lp-header">
              <div>
                <span className="lp-lp-badge">📺 ĐANG HỌC THỬ MIỄN PHÍ</span>
                <h4>{selectedLessonPlayer.lesson.name}</h4>
                <p>Khóa học: {selectedLessonPlayer.course.title} • {selectedLessonPlayer.course.teacherName}</p>
              </div>
              <button
                onClick={() => { setSelectedLessonPlayer(null); setLessonVideoPlaying(false); }}
                className="lp-lp-close-btn"
              >
                ✕
              </button>
            </div>

            {/* Main Area */}
            <div className="lp-lp-grid">
              {/* Left Side: Mock Video Player */}
              <div className="lp-lp-video-col">
                <div className="lp-lp-video-canvas">
                  <div className="lp-lp-video-graphic">
                    <span className="lp-lp-math-symbol symbol-1">f'(x) = 0</span>
                    <span className="lp-lp-math-symbol symbol-2">Δx → 0</span>
                    <span className="lp-lp-math-symbol symbol-3">y = ax + b</span>
                    {lessonVideoPlaying ? (
                      <div className="lp-lp-playing-ring">
                        <span className="lp-lp-waves"></span>
                        <div className="lp-lp-instructor-avatar">👨‍🏫</div>
                        <p style={{ color: '#fff', fontSize: '11px', marginTop: '10px', fontWeight: 'bold' }}>Thầy cô đang giảng giải chuyên đề...</p>
                      </div>
                    ) : (
                      <button className="lp-lp-play-trigger-btn" onClick={() => setLessonVideoPlaying(true)}>
                        ▶ BẮT ĐẦU XEM BÀI GIẢNG THỬ
                      </button>
                    )}
                  </div>

                  {/* Video Controls Bar */}
                  <div className="lp-lp-video-controls">
                    <button
                      onClick={() => setLessonVideoPlaying(!lessonVideoPlaying)}
                      className="control-btn"
                    >
                      {lessonVideoPlaying ? "⏸ Tạm dừng" : "▶ Phát"}
                    </button>
                    <div className="lp-lp-video-timeline">
                      <div className="timeline-bg">
                        <div className="timeline-fill" style={{ width: `${lessonVideoProgress}%` }}></div>
                        <div className="timeline-handle" style={{ left: `${lessonVideoProgress}%` }}></div>
                      </div>
                    </div>
                    <span className="time-display">05:42 / {selectedLessonPlayer.lesson.duration}</span>
                    <select
                      onChange={(e) => toast(`Tốc độ phát: ${e.target.value}x`, 'success')}
                      className="speed-select"
                    >
                      <option value="1">1.0x</option>
                      <option value="1.25">1.25x</option>
                      <option value="1.5">1.5x</option>
                      <option value="2">2.0x</option>
                    </select>
                  </div>
                </div>

                {/* Subtitle / Tip block */}
                <div className="lp-lp-video-subtitle">
                  <strong>💬 AI Phụ Đề & Ghi Chú Tự Động:</strong>
                  <p>“...khi khảo sát tính đơn điệu, chúng ta tính đạo hàm y'. Nếu y' &gt; 0 trên khoảng K thì hàm số đồng biến trên K, ngược lại nếu y' &lt; 0 thì nghịch biến...”</p>
                </div>
              </div>

              {/* Right Side: Tab Panel (Notes, Quiz, Chat) */}
              <div className="lp-lp-panel-col">
                <div className="lp-lp-tabs-bar">
                  <button className={`tab-btn ${activePlayerTab === 'notes' ? 'active' : ''}`} onClick={() => setActivePlayerTab('notes')}>📝 Tóm Tắt</button>
                  <button className={`tab-btn ${activePlayerTab === 'quiz' ? 'active' : ''}`} onClick={() => setActivePlayerTab('quiz')}>✍️ Luyện Tập</button>
                  <button className={`tab-btn ${activePlayerTab === 'chat' ? 'active' : ''}`} onClick={() => setActivePlayerTab('chat')}>🤖 Hỏi AI</button>
                </div>

                <div className="lp-lp-tab-content">
                  {/* Notes Tab */}
                  {activePlayerTab === 'notes' && (
                    <div className="lp-lp-notes animate-fade">
                      <h5>Tóm tắt cốt lõi bài học</h5>
                      <div className="notes-body">
                        <p><strong>1. Định lý tính đơn điệu:</strong> Cho hàm số f(x) có đạo hàm trên khoảng K.</p>
                        <ul>
                          <li>Nếu f'(x) ≥ 0, ∀x ∈ K (dấu bằng xảy ra tại hữu hạn điểm) ⇒ f(x) đồng biến trên K.</li>
                          <li>Nếu f'(x) ≤ 0, ∀x ∈ K (dấu bằng xảy ra tại hữu hạn điểm) ⇒ f(x) nghịch biến trên K.</li>
                        </ul>
                        <p><strong>2. Quy tắc xét tính đơn điệu:</strong></p>
                        <ol>
                          <li>Tìm tập xác định D.</li>
                          <li>Tính đạo hàm f'(x). Tìm các điểm x_i mà f'(x_i) = 0 hoặc f'(x_i) không xác định.</li>
                          <li>Sắp xếp các điểm theo thứ tự tăng dần và lập bảng biến thiên.</li>
                          <li>Kết luận khoảng đồng biến, nghịch biến.</li>
                        </ol>
                        <div style={{ marginTop: '16px', background: 'rgba(108,92,231,0.08)', border: '1px solid rgba(108,92,231,0.2)', padding: '10px', borderRadius: '8px', fontSize: '11.5px', color: 'var(--primary)' }}>
                          💡 **Casio Tip:** Dùng tính năng TABLE (Mode 8) nhập f(x), thiết lập Start, End, Step để xem trực tiếp chiều biến thiên giá trị f(x) tăng hay giảm!
                        </div>
                      </div>
                    </div>
                  )}

                  {/* Quiz Tab */}
                  {activePlayerTab === 'quiz' && (
                    <div className="lp-lp-quiz animate-fade">
                      <h5>Trắc nghiệm nhanh kiểm tra hiểu bài</h5>
                      <div className="quiz-questions-list">
                        {getLessonQuizQuestions(selectedLessonPlayer.course.id).map((qObj, qIdx) => {
                          const userAns = lessonQuizAnswers[qIdx];
                          return (
                            <div key={qIdx} className="quiz-q-block">
                              <p className="quiz-q-text"><strong>Câu {qIdx + 1}:</strong> {qObj.q}</p>
                              <div className="quiz-options-grid">
                                {qObj.options.map((opt, optIdx) => {
                                  let optClass = "";
                                  if (lessonQuizSubmitted) {
                                    if (optIdx === qObj.ans) optClass = "correct";
                                    else if (userAns === optIdx) optClass = "incorrect";
                                  } else if (userAns === optIdx) {
                                    optClass = "selected";
                                  }

                                  return (
                                    <button
                                      key={optIdx}
                                      className={`quiz-opt-btn ${optClass}`}
                                      disabled={lessonQuizSubmitted}
                                      onClick={() => {
                                        setLessonQuizAnswers(prev => ({ ...prev, [qIdx]: optIdx }));
                                      }}
                                    >
                                      {opt}
                                    </button>
                                  );
                                })}
                              </div>
                              {lessonQuizSubmitted && (
                                <div className="quiz-q-explanation">
                                  <strong>💡 Giải thích:</strong> {qObj.exp}
                                </div>
                              )}
                            </div>
                          );
                        })}
                      </div>

                      {!lessonQuizSubmitted ? (
                        <button
                          className="lp-btn lp-btn--accent lp-btn--full"
                          style={{ marginTop: '15px' }}
                          onClick={() => {
                            if (Object.keys(lessonQuizAnswers).length < getLessonQuizQuestions(selectedLessonPlayer.course.id).length) {
                              toast("Vui lòng hoàn thành tất cả câu hỏi trước khi nộp bài!", 'warning');
                              return;
                            }
                            setLessonQuizSubmitted(true);
                          }}
                        >
                          Nộp bài & Xem giải thích chi tiết
                        </button>
                      ) : (
                        <button
                          className="lp-btn lp-btn--outline lp-btn--full"
                          style={{ marginTop: '15px' }}
                          onClick={() => {
                            setLessonQuizAnswers({});
                            setLessonQuizSubmitted(false);
                          }}
                        >
                          Làm lại bài luyện tập
                        </button>
                      )}
                    </div>
                  )}

                  {/* Chat Tab */}
                  {activePlayerTab === 'chat' && (
                    <div className="lp-lp-chat animate-fade">
                      <div className="chat-history-container">
                        {lessonChatHistory.map((chat, idx) => (
                          <div key={idx} className={`chat-bubble ${chat.role}`}>
                            <div className="avatar-icon">{chat.role === 'assistant' ? '🤖' : '👤'}</div>
                            <div className="chat-text">{chat.content}</div>
                          </div>
                        ))}
                      </div>

                      <div className="preset-prompts-row">
                        <button onClick={() => { setLessonChatInput("Giải thích kỹ hơn định lý đơn điệu"); }}>❓ Giải thích định lý</button>
                        <button onClick={() => { setLessonChatInput("Mẹo bấm Casio cực trị"); }}>⚡ Mẹo Casio</button>
                      </div>

                      <form onSubmit={handleSendLessonChat} className="chat-input-form">
                        <input
                          type="text"
                          placeholder="Hỏi AI Tutor điều gì đó về bài học..."
                          value={lessonChatInput}
                          onChange={(e) => setLessonChatInput(e.target.value)}
                        />
                        <button type="submit">Gửi</button>
                      </form>
                    </div>
                  )}
                </div>
              </div>
            </div>

            {/* Footer Upgrade Prompt */}
            <div className="lp-lp-footer">
              <span>Đặc quyền Premium Pro mở khóa 120+ bài học & ngân hàng 10,000+ câu hỏi tự sinh.</span>
              <button onClick={() => { setSelectedLessonPlayer(null); setLessonVideoPlaying(false); onNavigateToAuth('signup'); }} className="lp-btn lp-btn--accent">
                Nâng cấp Premium Pro ngay
              </button>
            </div>
          </div>
        </div>
      )}

      {/* ── PRICING BANK QR CHECKOUT OVERLAY (GUEST PROMO SCAN) ── */}
      {pricingShowCheckout && (
        <div className="checkout-overlay" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 10000, background: 'rgba(15,23,42,0.85)', backdropFilter: 'blur(10px)' }}>
          <div className="checkout-modal lp-modal-neo animate-in" style={{ maxWidth: '440px', width: '90%', padding: '24px', textAlign: 'center', background: '#fff', color: '#111', borderRadius: '20px', border: '3px solid #000', boxShadow: '4px 4px 0 #000', position: 'relative' }}>
            <button
              onClick={() => setPricingShowCheckout(false)}
              style={{ position: 'absolute', top: '16px', right: '16px', border: 'none', background: 'none', color: '#111', fontSize: '20px', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', width: '32px', height: '32px', borderRadius: '50%', border: '2px solid #000', background: '#fff', boxShadow: '2px 2px 0px #000' }}
            >
              ✕
            </button>

            {selectedPackage === 'basic' ? (
              /* Basic Package Free Activation Mode */
              <div style={{ padding: '10px 0' }}>
                <div style={{ fontSize: '50px', marginBottom: '16px' }}>🌱</div>
                <span style={{ background: 'var(--primary-bg)', color: 'var(--primary)', fontSize: '10px', fontWeight: '800', padding: '4px 10px', borderRadius: '20px', display: 'inline-block', marginBottom: '14px', textTransform: 'uppercase' }}>🌱 GÓI TỰ HỌC MIỄN PHÍ</span>
                <h3 style={{ fontSize: '20px', fontWeight: '900', color: '#000', marginBottom: '10px' }}>Kích Hoạt Tài Khoản Trải Nghiệm</h3>
                <p style={{ fontSize: '13px', color: '#4B5563', marginBottom: '24px', lineHeight: '1.6' }}>Bạn đã lựa chọn gói Tự Học miễn phí. Vui lòng tạo tài khoản mới để lưu tiến trình làm đề thi thử và theo dõi lịch sử học tập.</p>

                <button
                  className="lp-btn lp-btn--primary lp-btn--full"
                  onClick={() => {
                    setPricingShowCheckout(false);
                    onNavigateToAuth('signup');
                  }}
                  style={{ padding: '12px', fontSize: '14px', fontWeight: 'bold' }}
                >
                  Tạo tài khoản học thử miễn phí →
                </button>
              </div>
            ) : (
              /* Paid Packages Bank QR Checkout Mode */
              <div>
                <span style={{ background: '#FFC229', color: '#000', fontSize: '10px', fontWeight: '800', padding: '4px 10px', borderRadius: '20px', display: 'inline-block', marginBottom: '14px', textTransform: 'uppercase' }}>💸 THANH TOÁN TỰ ĐỘNG</span>
                <h3 style={{ fontSize: '18px', fontWeight: '900', color: '#000', marginBottom: '8px' }}>Quét Mã VietQR Chuyển Khoản</h3>
                <p style={{ fontSize: '12px', color: '#4B5563', marginBottom: '20px' }}>Hệ thống tự động kích hoạt tài khoản ngay sau khi nhận được thông tin đối soát từ ngân hàng.</p>

                {/* Simulated VietQR SVG Graphic */}
                <div className="checkout-qr-placeholder" style={{ background: '#f8fafc', border: '2px solid #000', borderRadius: '12px', padding: '16px', display: 'inline-block', marginBottom: '20px', boxShadow: '2px 2px 0 #000' }}>
                  <svg viewBox="0 0 100 100" style={{ width: '150px', height: '150px' }}>
                    <rect x="5" y="5" width="90" height="90" fill="none" stroke="#000" strokeWidth="2" />
                    <rect x="10" y="10" width="25" height="25" fill="none" stroke="#000" strokeWidth="4" />
                    <rect x="15" y="15" width="15" height="15" fill="#000" />

                    <rect x="65" y="10" width="25" height="25" fill="none" stroke="#000" strokeWidth="4" />
                    <rect x="70" y="15" width="15" height="15" fill="#000" />

                    <rect x="10" y="65" width="25" height="25" fill="none" stroke="#000" strokeWidth="4" />
                    <rect x="15" y="70" width="15" height="15" fill="#000" />

                    <rect x="45" y="20" width="8" height="8" fill="#000" />
                    <rect x="40" y="40" width="12" height="12" fill="#000" />
                    <rect x="70" y="45" width="10" height="10" fill="#000" />
                    <rect x="25" y="45" width="8" height="8" fill="#000" />
                    <rect x="55" y="65" width="12" height="12" fill="#000" />
                    <rect x="75" y="75" width="10" height="10" fill="#000" />
                    <rect x="40" y="40" width="20" height="20" rx="4" fill="#6c5ce7" />
                    <text x="50" y="53" fill="#fff" fontSize="9" fontWeight="950" textAnchor="middle">EP</text>
                  </svg>
                </div>

                <div style={{ textAlign: 'left', background: '#f8fafc', padding: '12px 16px', borderRadius: '12px', border: '1.5px solid #e2e8f0', fontSize: '12.5px', color: '#1e293b', lineHeight: '1.6', marginBottom: '20px' }}>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Gói đăng ký:</span>
                    <strong>{selectedPackage === 'elite' ? 'Gói Elite Mentor (1 Năm)' : 'Gói Premium Pro (Trọn đời)'}</strong>
                  </div>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Ngân hàng:</span>
                    <strong>ACB (Á Châu)</strong>
                  </div>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Chủ tài khoản:</span>
                    <strong>TRAN VAN THUAN</strong>
                  </div>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Số tài khoản:</span>
                    <strong>18657431</strong>
                  </div>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Số tiền:</span>
                    <strong style={{ color: 'var(--accent-orange)' }}>
                      {((selectedPackage === 'elite' ? 1499000 : 599000) * (1 - pricingDiscountPercent / 100)).toLocaleString('vi-VN')}đ
                    </strong>
                  </div>
                  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
                    <span>Nội dung chuyển khoản:</span>
                    <strong style={{ color: '#ef4444' }}>EP {selectedPackage.toUpperCase()} {Math.floor(1000 + Math.random() * 9000)}</strong>
                  </div>
                </div>

                <div style={{ display: 'flex', alignItems: 'center', gap: '8px', justifyContent: 'center', fontSize: '11.5px', color: '#64748b', marginBottom: '16px' }}>
                  <span className="spinner-loader" style={{ border: '2px solid #cbd5e1', borderTop: '2px solid var(--primary)', borderRadius: '50%', width: '12px', height: '12px', display: 'inline-block', animation: 'spin 1s linear infinite' }}></span>
                  <span>Đang chờ nhận diện giao dịch tự động...</span>
                </div>

                <button
                  className="lp-btn lp-btn--accent lp-btn--full"
                  onClick={() => {
                    setPricingShowCheckout(false);
                    toast(`Đăng ký thành công! Đặc quyền ${selectedPackage === 'elite' ? 'Elite Mentor' : 'Premium Pro'} sẽ được kích hoạt sau khi xác nhận giao dịch.`, 'success');
                    onNavigateToAuth('signup');
                  }}
                  style={{ padding: '12px', fontSize: '13.5px', fontWeight: 'bold' }}
                >
                  Xác nhận đã chuyển khoản thành công
                </button>
              </div>
            )}
          </div>
        </div>
      )}


      {/* ── GUEST MOCK EXAMS SIMULATOR MODAL OVERLAY ── */}
      {selectedExamSimulator && (
        <div className="checkout-overlay" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 10000, background: 'rgba(15,23,42,0.85)', backdropFilter: 'blur(10px)' }}>
          <div className="checkout-modal lp-modal-neo animate-in" style={{ maxWidth: '800px', width: '95%', padding: '24px', textAlign: 'left', background: '#fff', color: '#111', borderRadius: '20px', border: '3px solid #000', boxShadow: '5px 5px 0 #000', position: 'relative', maxHeight: '90vh', overflowY: 'auto' }}>
            <button
              onClick={() => setSelectedExamSimulator(null)}
              style={{ position: 'absolute', top: '16px', right: '16px', border: 'none', background: 'none', color: '#111', fontSize: '20px', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', width: '32px', height: '32px', borderRadius: '50%', border: '2px solid #000', background: '#fff', boxShadow: '2px 2px 0px #000' }}
            >
              ✕
            </button>

            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '2.5px solid #000', paddingBottom: '12px', marginBottom: '20px' }}>
              <div>
                <span style={{ background: '#FFC229', color: '#000', fontSize: '11px', fontWeight: '800', padding: '4px 10px', borderRadius: '20px', display: 'inline-block', marginBottom: '6px' }}>📝 ĐỀ THI MẪU THỬ MIỄN PHÍ</span>
                <h3 style={{ fontSize: '18px', fontWeight: '950', color: '#000', margin: 0 }}>{selectedExamSimulator.name}</h3>
              </div>
              <div style={{ padding: '8px 16px', border: '2px solid #000', borderRadius: '12px', background: '#FFF2F2', fontWeight: '900', fontSize: '15px', display: 'flex', alignItems: 'center', gap: '6px', boxShadow: '2px 2px 0 #000' }}>
                ⏱️ {Math.floor(examTimeLeft / 60)}:{(examTimeLeft % 60).toString().padStart(2, '0')}
              </div>
            </div>

            <div style={{ display: 'grid', gridTemplateColumns: '3fr 1fr', gap: '20px' }}>
              {/* Question area */}
              <div style={{ border: '2px solid #000', borderRadius: '16px', padding: '20px', background: '#FCFBFA', boxShadow: '3px 3px 0 #000' }}>
                <span className="badge-pill" style={{ background: 'var(--primary-bg)', color: 'var(--primary)', fontWeight: '800', fontSize: '11px' }}>
                  Câu hỏi {examActiveQuestion + 1} / {selectedExamSimulator.questions.length}
                </span>
                <p style={{ fontSize: '15px', fontWeight: '850', color: '#000', margin: '14px 0 20px', lineHeight: 1.5 }}>
                  {selectedExamSimulator.questions[examActiveQuestion]?.question}
                </p>

                <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
                  {selectedExamSimulator.questions[examActiveQuestion]?.options.map((opt) => {
                    const isSelected = examAnswers[selectedExamSimulator.questions[examActiveQuestion].id] === opt.key;
                    return (
                      <button
                        key={opt.key}
                        onClick={() => {
                          setExamAnswers(prev => ({
                            ...prev,
                            [selectedExamSimulator.questions[examActiveQuestion].id]: opt.key
                          }));
                        }}
                        style={{
                          width: '100%',
                          textAlign: 'left',
                          padding: '12px 16px',
                          border: '2px solid #000',
                          borderRadius: '12px',
                          background: isSelected ? 'rgba(108, 92, 231, 0.08)' : '#fff',
                          fontWeight: isSelected ? '900' : '700',
                          color: '#000',
                          cursor: 'pointer',
                          display: 'flex',
                          alignItems: 'center',
                          gap: '12px',
                          transition: 'all 0.15s',
                          boxShadow: isSelected ? 'none' : '2px 2px 0px #000',
                          transform: isSelected ? 'translate(2px, 2px)' : 'none'
                        }}
                      >
                        <span style={{ display: 'inline-flex', width: '26px', height: '26px', border: '2px solid #000', borderRadius: '50%', alignItems: 'center', justifyContent: 'center', background: isSelected ? 'var(--primary)' : '#fff', color: isSelected ? '#fff' : '#000', fontWeight: '900', fontSize: '12px' }}>
                          {opt.key}
                        </span>
                        <span>{opt.value}</span>
                      </button>
                    );
                  })}
                </div>
              </div>

              {/* Sidebar grid of questions */}
              <div>
                <h4 style={{ fontSize: '12.5px', fontWeight: '900', textTransform: 'uppercase', marginBottom: '10px', color: '#666' }}>Danh sách câu hỏi</h4>
                <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '8px', marginBottom: '20px' }}>
                  {selectedExamSimulator.questions.map((q, idx) => {
                    const answered = examAnswers[q.id] !== undefined;
                    const isActive = examActiveQuestion === idx;
                    return (
                      <button
                        key={q.id}
                        onClick={() => setExamActiveQuestion(idx)}
                        style={{
                          padding: '8px 0',
                          border: '2px solid #000',
                          borderRadius: '8px',
                          background: isActive ? '#FFE259' : (answered ? 'rgba(34, 197, 94, 0.15)' : '#fff'),
                          fontWeight: '900',
                          cursor: 'pointer',
                          boxShadow: '1.5px 1.5px 0 #000'
                        }}
                      >
                        {idx + 1}
                      </button>
                    );
                  })}
                </div>

                <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
                  <button
                    onClick={() => {
                      toast('Bạn cần đăng ký hoặc đăng nhập tài khoản để nộp bài và xem phân tích kết quả từ AI!', 'warning');
                      setSelectedExamSimulator(null);
                      onNavigateToAuth('signup');
                    }}
                    style={{
                      width: '100%',
                      padding: '12px',
                      background: '#22C55E',
                      color: '#fff',
                      border: '2px solid #000',
                      borderRadius: '12px',
                      fontWeight: '900',
                      fontSize: '13.5px',
                      cursor: 'pointer',
                      boxShadow: '3px 3px 0 #000',
                      textAlign: 'center'
                    }}
                  >
                    Nộp bài thi 🚀
                  </button>
                  <button
                    onClick={() => {
                      if (examActiveQuestion < selectedExamSimulator.questions.length - 1) {
                        setExamActiveQuestion(prev => prev + 1);
                      }
                    }}
                    disabled={examActiveQuestion === selectedExamSimulator.questions.length - 1}
                    style={{
                      width: '100%',
                      padding: '10px',
                      background: '#fff',
                      color: '#000',
                      border: '2px solid #000',
                      borderRadius: '12px',
                      fontWeight: '800',
                      fontSize: '12px',
                      cursor: 'pointer',
                      boxShadow: '2px 2px 0 #000',
                      opacity: examActiveQuestion === selectedExamSimulator.questions.length - 1 ? 0.5 : 1
                    }}
                  >
                    Câu tiếp theo →
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      )}


      {/* ── FOOTER ── */}
      {!['flashcards', 'ai-tutor'].includes(activeLandingView) && !isExamTaking && (
        <footer className="lp-footer">
          <div className="lp-container">
            <div className="lp-footer__grid">
              <div className="lp-footer__brand">
                <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 14 }}>
                  <div className="lp-logo-box" style={{ width: 36, height: 36, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 0, background: 'none', boxShadow: 'none' }}>
                    <img src={sunLogoImg} alt="EduPath AI" style={{ width: '32px', height: '32px', objectFit: 'contain' }} />
                  </div>
                  <span style={{ fontWeight: 800, fontSize: 17, color: '#fff' }}>EduPath AI</span>
                </div>
                <p>Nền tảng luyện thi THPTQG thích ứng AI — học thông minh, đạt điểm cao.</p>
              </div>
              <div className="lp-footer__col">
                <h4>Khóa học</h4>
                <a href="/courses" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } }}>Toán Học</a>
                <a href="/courses" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } }}>Vật Lý</a>
                <a href="/courses" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } }}>Tiếng Anh</a>
                <a href="/courses" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/courses'); } else { setActiveLandingView('courses'); } }}>Hóa Học</a>
              </div>
              <div className="lp-footer__col">
                <h4>Tính năng</h4>
                <a href="/flashcards" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/flashcards'); } }}>Flashcard</a>
                <a href="/mock-exams" onClick={(e) => { e.preventDefault(); if (!currentUser) { toast("Vui lòng đăng nhập để sử dụng chức năng thi thử!", 'warning'); if (onNavigateToAuth) onNavigateToAuth('login'); } else { if (navigateTo) { navigateTo('/mock-exams'); } else { setActiveLandingView('exams'); } } }}>Thi thử</a>
                <a href="/forum" onClick={(e) => { e.preventDefault(); if (navigateTo) { navigateTo('/forum'); } else { setActiveLandingView('forum'); } }}>Cộng đồng</a>
                <a href="#stats" onClick={(e) => { e.preventDefault(); setActiveLandingView('about'); }}>Về chúng tôi</a>
              </div>
              <div className="lp-footer__col">
                <h4>Hỗ trợ</h4>
                <a href="#">Trung tâm trợ giúp</a>
                <a href="#">Liên hệ</a>
                <a href="#">Chính sách bảo mật</a>
              </div>
            </div>
            <div className="lp-footer__bottom">
              <span>© 2026 EduPath AI. Bản quyền thuộc về EduPath.</span>
              <span>Nhóm SWP391 NHOM1</span>
            </div>
          </div>
        </footer>
      )}
    </div>
  );
}
